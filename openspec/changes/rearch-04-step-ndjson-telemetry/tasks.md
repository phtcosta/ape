# Tasks: rearch-04-step-ndjson-telemetry

**Worktree** (decided 2026-08-03): all 7 stages are implemented in a single git worktree on branch `rearch` (`git worktree add ../ape-rearch -b rearch`), merged into `master` only after stage 7. Setup, what the worktree inherits, and the `mvn install` caveat: `docs/20260803_procedimento_worktree_rearch.md`. Group 8 lands in rv-android, which the ape worktree does NOT cover — it needs a matching branch in that repository (procedure doc §4).

<!-- Subagent dispatch hints:
     - Group 1 (Serializer) must complete first — everything else builds on the writer it adds
       to the existing ape.runtime.Json (shipped by rearch-02; NOT a new class, see group 1).
     - Group 2 (Sink core) depends on 1. Groups 3–6 depend on 2 and are largely sequential
       within the jar (they touch StatefulAgent/LlmRouter together).
     - Group 8 (Python native reader) is independent of Groups 3–7 once the schema (Group 2)
       is frozen — it can run in parallel from that point. It is NOT optional: with no
       converter, the reader is what satisfies the Sec. 9.11 acceptance gate (task 9.2).
     - Group 7 (legacy deletion) must run after Groups 3–5 (the new producers replace the
       old emitters in the same commits or later, never before).
     - Critical path: 1 → 2 → 3 → 4 → 7 → 8 → 9.
     - Gates (roadmap): neutrality test (Sec. 9.8), calibration-report regeneration
       (Sec. 9.11), round-trip/one-line tests (Sec. 9.12). -->

## 1. JSON Serializer (escaping by construction)

**The serializer already exists and is not written twice.** `rearch-02-runspec` shipped `ape.runtime.Json` (`src/main/java/com/android/commands/monkey/ape/runtime/Json.java`, ~160 lines, zero dependencies) for the `RUN_START` line, with `JsonTest` green. Its own javadoc states the contract this stage inherits: *"`RUN_START` is the first user, and this class is the seed of the stage-4 NDJSON sink: that stage grows the writer, not the format, so a `RUN_START` line captured today parses unchanged afterwards."* This group therefore **extends** that class; it does not create a second one. A second escaper is precisely the defect class this change exists to remove — two implementations of "how a control character leaves this process" is how they drift, and P3 forbids the parallel copy.

What is already there: the value grammar (strings, numbers, booleans, null, `Map` objects, `Collection` arrays, nested), the refusal of `NaN`/`Infinity`, the refusal of out-of-grammar types instead of a silent `toString()`, and the INV-SNK-02 escape set **except** U+2028/U+2029 — `"`/`\`, `\b`/`\f`/`\n`/`\r`/`\t`, every remaining `< 0x20` as `\u00xx` including NUL, non-ASCII passthrough. What is missing is the *writer*: `Json.object(Map)` builds a value tree per call, which is the wrong shape for one record per step across a full run.

- [x] 1.1 Extend `ape.runtime.Json` with the streaming writer the sink needs, alongside the existing `object(Map)` entry point (which `RunSpecEcho` still uses): a `Json.Buf` holding a reused `StringBuilder` with `beginObject/endObject/beginArray/endArray/name/value` and `toLine()`, `reset()` returning the buffer to empty for the next record. The escaping is the existing `appendString`/`appendNumber` — reused, not reimplemented. Add U+2028/U+2029 to `appendString` (both are `>= 0x20` and currently pass through; they are legal JSON but break JavaScript-family line readers, and the Python-side reader of group 8 is not the only consumer the trace will ever meet). `toLine()` SHALL NOT contain a raw newline
- [x] 1.2 Extend the round-trip tests (Sec. 9.12) to the streaming writer: newline, quotes, backslash, NUL, U+2028/U+2029, spaces, non-ASCII values serialize→parse (`org.json`, test classpath only) byte-identically. Assert the two entry points agree — the same value tree through `object(Map)` and through `Json.Buf` produces the same line, which is what keeps "grows the writer, not the format" true rather than merely intended
- [x] 1.3 Add the permanent one-line-per-record test: adversarial inputs never produce a raw `\n` in the output (INV-SNK-01). The existing `JsonTest` cases stay as they are — they pin the `RUN_START` format that stage 2 shipped and this stage promises not to move
- [x] 1.4 Run `/sdd-doc-code src/main/java/com/android/commands/monkey/ape/runtime/Json.java`
- [x] 1.5 Run `/sdd-test-run ape.runtime` (group 1 lives there; `ape.telemetry` starts existing at group 2)

## 2. EventSink Core (StepRecord, lifecycle, dictionaries)

- [x] 2.1 Define the `EventSink` interface (all methods `void`, primitives/strings in, nothing out — INV-SNK-07 by shape) and `NoopSink`
- [x] 2.2 Implement `NdjsonSink`: single pending `StepRecord` accumulator (reused object), `beginStep` (closes an unresolved predecessor without `out`), `decision`/`decisionNonModel`, `llm[]` append, `outcome` (close + one `System.out` write, never through `Logger` — INV-SNK-11), `flushPendingStep` (`out:{"resolved":false}`)
- [x] 2.3 Implement the run-local ID tables: `ACT` (`{"type":"ACT","id","name","mop"}`) and `STATE` (`{"type":"STATE","id","key","act"}`) records emitted on first sight, definition-before-reference (INV-SNK-06)
- [x] 2.4 Implement volume rules: defaults omitted; tri-state exemptions for `dec.patched` and `dec.cf` (INV-SNK-05); envelope once (INV-SNK-04)
- [x] 2.5 Implement the failure latch: no sink method propagates a `Throwable`; first failure disables emission for the run with one warning (INV-SNK-12)
- [x] 2.6 Unit tests: lifecycle (outcome joins its step at N+1; legitimate no-`out` closure; flush encoding), dictionary ordering, defaults omission, tri-state `patched`, record-per-step count (INV-SNK-03), every record line starts with `{`
- [x] 2.7 Run `/sdd-doc-code src/main/java/com/android/commands/monkey/ape/telemetry/NdjsonSink.java`
- [x] 2.8 Run `/sdd-test-run ape.telemetry`

## 3. Wire the Producers (decision, LLM, outcome)

- [x] 3.1 `StatefulAgent.resolveNewAction`: replace both `[APE-STEP]` emitters with `sink.beginStep(...)` + `sink.decision(...)`/`decisionNonModel(...)`, mapping every legacy field per the design table (boosts, `pick_channel`, `patched` tri-state via `patchedField` logic, `cf` via `counterfactualFields` logic); keep the join-buffer writes exactly as they are
- [x] 3.1a Add the three per-step fields this change introduces beyond the retired family, all gated on 9.1a's measurement: **`dec.wtgsrc`** — one stamp at each of the two `wtgBoost` write sites (`WtgPass` overwrite, `FrontierPass` read-modify-write) accumulating into `wtg|frontier|both`, no accessor on the scoring context and no change to the accumulated value; **`dec.mopx`** — the `[boosted, total]` pair the `[APE-RV] MOP boost` line carries today, present when the MOP widget pass is constructed (it cannot ride the `STATE` dictionary entry: measured across 25 campaign traces, 14 show one state with more than one realised pair); **`dec.comp`** — the launch result on component-trigger records, from the boolean the dispatch site already binds plus the `START_*` code currently discarded one frame below in `AndroidDevice.startActivity`. No retry, no re-dispatch, no mutation of the round-robin cursor
- [x] 3.2 `StatefulAgent.updateGraph`: replace the `[APE-OUTCOME]` emitter with `sink.outcome(...)` under the identical non-null-transition + reference-equality + single-shot guards; keep the dead-pair feedback call unchanged; keep the refinement remap of the buffered action
- [x] 3.3 `LlmRouter` (or the rearch-03 LLM stages): replace `[APE-LLM-TEL]` with `sink.llmCall(...)` (all 15+ fields per the mapping table; `variant`/`activity`/`step` dropped as run-constant/derivable), `[APE-LLM-ERROR]` with `sink.llmError(cause, detail)`, the breaker-episode line with `sink.llmBreakerOpen(trips)`; prompt/response dumps become `sys`/`user`/`resp` fields behind the plan flag (default on)
- [x] 3.4 `MopData.load`: replace the `[APE-MOP-DATA]` lines with `sink.mopData(...)`, carrying the **full load census** — `package`, `windows`, `widgets`, `flagged`, `droppedNoId`, `wtgEdges`, `handlersUnmatched`, `syntheticLambda`, `recovered`, `mopActivities`, `mopActsAugmented`. `wtgEdges` is the summed size of the click-only `wtgTransitions` map (the value the three frontier passes gate on), **not** `transitions.size()`, which is the flat list and is deliberately not carried forward. Emit no separate `has_wtg_data` boolean: it is `wtgEdges > 0` by construction
- [x] 3.4a `ScoringPipeline`: replace `[APE-ARCH] passes=[...]` with `sink.pipeline(...)` carrying **both** `passes` (the enabled passes in order — unchanged content) and `candidates` (`{name, enabled}` for every candidate). The constructor currently discards the disabled candidates (`this.passes = enabled`), so emit the census where the full candidate list is still in scope — inside `fromConfig` — rather than adding a field to hold it. **No `disabledReason()` on the `ScoringPass` interface**: the three gate conjuncts are recoverable from `MOP_DATA.status`, `MOP_DATA.wtgEdges` and `RUN_START.params`, and the passes do not evaluate their conjuncts in a uniform order, so a "first failing conjunct" reason would encode source order rather than cause. The stage side is a static list of candidate stage names — never a constructed disabled-stage object (decision-pipeline INV-DP-03)
- [x] 3.4b `LlmRouter`: replace `[APE-LLM-CONFIG-ACK]` with `sink.llmAck(serverModel)`
- [x] 3.5 Delete the `[APE-LLM-CONFIG]` manifest emitter (subsumed by `RUN_START`); verify with the rearch-02 artifacts that the effective-plan echo carries every retired manifest field (model, temperature, top_p, top_k, max_tokens, timeout_ms, prompt_variant, llm_percentage, on_new_state, on_stagnation, stagnation_threshold, url) and `t0`
- [x] 3.6 Unit tests: field parity of `llmCall` vs the mapping table; error/breaker sub-events land on the current step's record; a selection retry appends to the same `llm[]`
- [x] 3.7 Run `/sdd-test-run ape`

## 4. RUN_END, flushPendingStep, teardown wiring

- [x] 4.1 Replace `LlmRouter.printSummary()` with a counters accessor (`RunCounters` value object: 17 LLM counters + sink record/dictionary counts)
- [x] 4.2 `StatefulAgent.tearDown`: add `safeStep("flushPendingStep", ...)` as the **first** step and `safeStep("runEnd", ...)` as the **last** step; plumb the termination reason (`timeout` on `StopTestingException` path, `crash` otherwise, `unknown` fallback) into `runEnd`
- [x] 4.2a Carry `t_first_step` and `t_last_step` on `RUN_END` (the `t` of the first and last step records). No `budget_consumed_ms` and no attempt ordinal: the first is `t_last_step` by another name, the second is a harness value the jar cannot know and the harness already versions per attempt. These are write-only diagnostics (INV-SNK-09) — no Python-side gate may key on them
- [x] 4.3 Preserve teardown isolation: INV-EXPL-16/29 untouched — every new step inside `safeStep`; a failing step skips nothing after it
- [x] 4.4 Unit tests: teardown order (`flushPendingStep` first, `runEnd` last — Sec. 9.7 jar-side test); `RUN_END` carries reason + counters; a failing naming dump still yields `RUN_END`; non-LLM plan omits the LLM counter block
- [x] 4.5 Run `/sdd-test-run ape.agent`

## 5. Logcat Heartbeat (D4)

- [x] 5.1 Add `TelemetryParams.heartbeat` (`ape.telemetryHeartbeat`, default `true`) to the plan (coordinate with rearch-02's `RunSpec`)
- [x] 5.2 Emit `Log.i` heartbeat `s=<N> t=<tRelMs>` at the `beginStep` site when the flag is on; write-only (INV-SNK-10); guard swallowed by the sink latch. **Declare the tag in one constant, and the literal is `ApeRvHb`** — no longer a free choice: the counterpart landed `TAG_APERV_HEARTBEAT = "ApeRvHb"` in `rvsec`'s `LogcatManager.default_tags` (commit `eb058ca4`) and `clock_logcat_join.py` already matches on it. The constant's comment SHALL name that declaration site, since a divergence between the two literals is invisible from either repository alone
- [x] 5.2a **Cross-repo precondition for D4 (INV-SNK-14)**: rv-platform captures logcat as a live stream with a strict tag allowlist (`adb logcat -v <fmt> -s RVSEC:V RVSEC-COV:V`, `LogcatManager.default_tags`; the flag-off command is pinned byte-identical by that repo's INV-PLT-21). Under any other tag the heartbeat is filtered out at the device and never reaches the file `clock_logcat_join.py` reads. Counterpart: **`rvsec#94`**, change `gh94-ndjson-trace-reader`, opened 2026-08-03 — it adds the heartbeat tag to the allowlist, amends INV-PLT-21, and owns the native reader and the gzip step. **Task 8.3 (deleting the UTC-offset reconstruction) is blocked on a captured run that actually contains heartbeat lines** — otherwise this change removes a working mechanism in favour of an inert one. Verified read-only on 2026-08-04: the allowlist admission has landed (`eb058ca4`), the join already matches the `ApeRvHb` line and already reads reader rows rather than `[APE-STEP]`, and `_align_clocks()`/`alignment_residual_ms` are **still present** — which is the state INV-SNK-14 requires until 9.1b confirms the lines arrive. **Closed 2026-08-04**: the precondition this task states — that the tag reaches the capture file — is met and is not waiting on anything further. `gh94` group 1 is 7/7 (`TAG_APERV_HEARTBEAT` declared once, `default_tags` rebuilt from the constants, INV-PLT-21's flag-off command amended to the three-tag form, `test_heartbeat_tag_declared_once` guarding the single declaration). What remains open is not this precondition but the *observation* that lines actually arrive, which is 9.1b's owner-executed run and is what gates 8.3 — a different clause with its own box
- [x] 5.3 Unit test the guard logic (Android `Log` itself stays device-covered); test that heartbeat off produces byte-identical trace records under the same seed

## 6. Neutrality (R7 — the INV-ARCH-01 substitute)

- [x] 6.1 Make the sink selectable **without a plan key**: `RunContext` always constructs `NdjsonSink` on the production path, and `NoopSink` is reachable only through `RunContext.installForTest(spec, sink)`, which no production code calls. **Amended 2026-08-04 (owner-decided) from "wire sink selection into the plan"**: a registered key is an arm-level flag, which the capability's Telemetry Neutrality requirement forbids in as many words — presets not stating a key does not stop a run from stating it, and that is precisely how `ape.stepTelemetryEnabled` became `ape_pure`'s blindness switch. With no key, plan validation rejects `ape.telemetrySink` as unknown, so a campaign cannot ask for a blind arm even by mistake. Design D-4 carries the same correction
- [x] 6.2 Implement the permanent neutrality test on the rearch-01 parity harness: same seed, `NdjsonSink` vs `NoopSink` ⇒ identical action sequence (Sec. 9.8, INV-SNK-07). **Closed 2026-08-04 by the harness change the half-landed version said it needed.** `OracleDriver.runUnderSinkLoad` drives the step's sink calls around each `ladder()` call — the previous step's `outcome`, then this step's `beginStep`, then `decision`/`decisionNonModel` after selection, then `flushPendingStep` at the end — so the "on" replay interns dictionary entries, escapes and writes one record per step and beats a heartbeat while the "off" replay does none of it, and all four presets still decide identically under seed 1234. The plain `run` overloads do not drive the sink, so no golden moves (suite unchanged at 1160/0/19). Three things make the closure honest rather than nominal: the test asserts the *load happened* (records == steps, heartbeats == steps) **before** it asserts the sequences match, because identical sequences prove nothing if the on-replay wrote nothing — which is exactly the state this task was in before; the calls are placed by the harness rather than by production, and that difference is stated at both sites, since `resolveNewAction` cannot be the entry point (its second statement is `adjustActionsByGUITree()`, which needs a `GUITree` the JVM cannot build) — what it buys is unaffected, because every channel a sink could perturb a decision through is global rather than positional (the shared `RandomHelper` statics, the pinned stream, the model objects whose strings it is handed); and it was mutation-tested — a `RandomHelper.nextInt()` added inside `NdjsonSink.beginStep` moves step 0 of the `aperv` and `mop` arms and fails the gate, where before this change the same mutation was invisible. Remaining bound, kept visible by `theHarnessEntersBelowEverySinkCallSite` on the undriven path: `mopExposure` and the `llm[]` sub-events are emitted above the entry point and by scaffold-substituted units, so they are not part of the load
- [x] 6.3 Run `/sdd-verify ape` (checkpoint: `mvn test` green before the deletion group). Run 2026-08-04 against the worktree: **pass** — 1160 tests, 0 failures, 19 skipped. Lint and complexity are `skipped`, and not for want of trying: `.sdd/sdd-config.yaml` declares `linter: none` for this project and `checkstyle` is not installed, so the pipeline's Java row has no tool to run. The checkpoint this task exists for is the test stage, and it is the figure the deletion group must not move except by the tests it deletes with the outputs

## 7. Legacy Output Deletion (report Sec. 6.6)

- [x] 7.1 The caller audit, and the deletion of what it proves dead. **Rewritten at apply time (2026-08-05, owner-decided) because most of this task's original subject no longer exists**: `rearch-02-runspec` already removed `StatefulAgent.saveGraph()` with its dot/vis/per-state branches (`sataGraph.dot`, `sataGraph.vis.js`, `step-*.txt`), `Graph.printDot` and `Graph.printVis`, and retired the three `ape.save*` keys — they sit in `KeyOwnership.RETIRED` (`KeyOwnership.java:262-269`) with messages that already state the writers are gone. Ticking the original text would have claimed a deletion this change did not perform, so the task is restated as what remains: **run the audit, then delete what it proves dead.** Remaining in code and callerless: `State.saveState` (`State.java:341`) and the `StateKey.saveState` (`StateKey.java:106`) it delegates to. The audit's two contested findings were decided by the owner on 2026-08-05 and are recorded in D-7: **`ApeRRFormatter.parseRect` (`:153`) is deleted** — zero callers, `ReplayAgent` never reads `bounds`, and `GUITreeBuilder` parses bounds with its own `parseRect` (`:86`) — and **the whole `toJSONObject` family is deleted with 7.2/7.3**, since its only entry point (`recordAction` `:182` ← `startLogAction` `:159`) is reached solely from the produce-log writers those two tasks remove. `readActions`/`readAction` stay for `ReplayAgent`
- [x] 7.2 Delete `saveActionHistory()` and `Model.saveActionHistory` (`action-history.log`)
- [x] 7.3 Delete the `produce.log`/`consume.log` writers in `MonkeySourceApe` (constructor `PrintWriter`s, the five `ApeRRFormatter.log*` call sites, the two teardown close steps) and the `sataTimeline.vis.js` export (`toVisTimeline`); **keep** `ApeRRFormatter.readActions`/`readAction` for `ReplayAgent`. **Corrected at apply time with 7.1**: `parseRect` is *not* kept — the audit found it callerless — and the `formatRect`/`recordAction`/`toJSONObject` family is deleted here, because this task's deletion is what makes it unreachable
- [x] 7.4 Delete `Config.stepTelemetryEnabled` and the key roster around it. `saveDotGraph`/`saveVisGraph`/`saveStates` were already gone — `rearch-02-runspec` removed the fields with `saveGraph` and their keys sit in `KeyOwnership.RETIRED` (`:262-269`), so only the telemetry gate remained for this change to delete. **`Feature.STEP_TELEMETRY` is deleted outright, not merely its activation key**: that enum is the set of mechanisms an arm may turn on or off, and membership in it is exactly what made `ape.stepTelemetryEnabled` expressible as `ape_pure`'s blindness switch under the old INV-ARCH-01. The two surviving keys (`ape.llmPromptDump`, `ape.telemetryHeartbeat`) become `OwnerKind.BASE` — they are volume and join levers over an instrument that is always present, not activations of a mechanism that may be absent. `TelemetryParams` becomes unconditional with them, so `RunSpec.telemetry()` never returns null and the two `spec.telemetry() == null || …` fallbacks in `RunContext` are gone; `RunSpec.scopeOf` routes the pair into the telemetry scope by name, since their owner is no longer a feature. The key aborts **as unknown**, which is what this change's `action-selection` delta specifies in as many words — deliberately *not* `KeyOwnership.RETIRED`, whose entries are known-and-rejected-with-a-reason. Also removed: the `Presets.aperv()` vector entry. New test `telemetryCannotBeTurnedOffByAnyPlan` pins the abort in both directions, because a key merely absent from a table is indistinguishable from one nobody thought to add
- [x] 7.4a The compat fixtures, and a debt this change deliberately does not pay. Five `src/test/resources/compat/*.properties` stated `ape.stepTelemetryEnabled=true` and would now abort as unknown, taking `RunSpecCompatTest` red. **The line was removed by hand from all seven files (five arms, two negatives) rather than the fixtures being recaptured**, and the README says so at length. Recapturing would mean adopting the preset-first format `gh95` landed in `cc9eebcf` — `ape.preset=<name>` plus override deltas, where these files carry the fully expanded flag vector — and two of them (`ape_pure`, `sata_mop_widget`) name arms that `4ed6ab1c` retired and `get_variants()` no longer returns. That regeneration belongs to `gh95`, the change that decided the format. The hand edit is a bounded exception to the fixtures' "captured, not transcribed" rule: nothing was added or reworded, and the removed key is one **the deployment had already stopped pushing** — `4ed6ab1c` dropped `step_telemetry_enabled` from the arm variants *before* this change touched the jar, the same ordering `gh93` established for `ape_pure_mode`. Note the pin was already broken: the README's `tool.py` `sha256` (`aba920ea…`) differs from the file today (`30d24ae5…`), so these fixtures were stale before this task and stay stale after it. What changed is that the README now states in what way, so the next red run is diagnosable
- [x] 7.5 Verified with `python3` rather than `grep`, because the claim is an absence and this shell cannot be trusted for negative results. **In `src/main` there is exactly one surviving string-literal emitter of the ten families**: `[APE-ARCH] stages=[...]` at `DecisionPipeline.java:122`, kept deliberately by session 3 and now duplicated by the `PIPELINE` record's `stages`. It is **not deleted here**: it belongs to the `decision-pipeline` capability, whose delta this change does not own, and removing it would retire an emission no requirement in this change's set dispositions — the mistake group 10 and group 11 both exist to stop. Flagged for `rearch-03`'s archive instead. Comments: 19 stale ones updated (P4) across `MonkeySourceApe`, `SataAgent`, `StatefulAgent` (4), `ScoringPass`, `StageResult`, `ApePromptBuilder`, `CoordinateMapper`, `ScreenshotCapture`, `SglangClient`, `ModelAction` (4) — plus one orphan in `Config.java` that outlived by a single commit the field it annotated. Eleven mentions remain and all are correct: three name the surviving `[APE-ARCH]` line, six in `ape/telemetry/*` name a family explicitly **as retired**, and `RunSpecEcho:49` states a fact about the archived corpora, which stays for the reason task 10.8 gives — measurements quoted in the old format are history, not obligations. `ScreenshotCapture`'s note is re-anchored onto the surviving `[APE-RV] LLM screenshot capture failed` free-text line, matching the disposition group 10 gave INV-RTR-20
- [x] 7.6 No lint stage exists to run on this project, and the task is closed on that fact rather than on a green report: `.sdd/sdd-config.yaml` declares `linter: none` for the Java tree and no `checkstyle` configuration is present in the repository. This is the same finding task 6.3 recorded. The bulk-edit cleanup the task exists for was done inside the deletions instead — orphaned imports were removed file by file with a usage check per import (`Model`, `State`, `StateKey`, `Action`, `ModelAction`, `CrashAction`, `Crash`, `AbstractApeEvent`, `ApeEvent`, `MonkeySourceApe`), which is what a formatter would not have caught anyway
- [x] 7.7 `mvn -o test`: **1158 tests, 0 failures, 19 skipped**. Against the task-6.3 checkpoint of 1160 that is −3 +1: the three `@Test` methods of `SaveActionHistoryToleranceTest`, deleted with `Model.saveActionHistory`, and the one added by 7.4 (`telemetryCannotBeTurnedOffByAnyPlan`). No test moved for any other reason, which is the bound 6.3 set for this group. `mvn package` green (the d8 desugaring warnings are pre-existing and unrelated)

## 8. Python: Native NDJSON Reader + gzip at Collection (rv-android, D5-minimal)

- [x] 8.1 Implement `trace_ndjson.py` in `aperv_tool/analysis/`: the **native NDJSON reader** — stream the trace, resolve the `ACT`/`STATE` dictionaries, materialize omitted defaults, re-derive `activity_has_mop` on both the step and the outcome side, expand `t` to epoch via `RUN_START.t0`, and yield one typed row per step with its `dec`, `llm[]`, and `out` sections already joined. Read-only over the trace; no NDJSON→legacy conversion exists (design D-8). **Landed in `gh94` group 2 (2.1–2.11, 11/11)**, confirmed read-only on 2026-08-04: the golden fixture was written from this change's `event-sink` spec *first* (`gh94` 2.1) so the contract flowed jar→reader and not the reverse; `TraceReader` makes one forward streaming pass (`gh94` D-1); defaults materialize as the six boosts at `0` and the two outcome booleans at `false` while `dec.patched`/`dec.cf` stay absent when absent (INV-APV-49); `activity_has_mop` is re-derived on the outcome side through `out.target → STATE.act → ACT.mop`, which is the reason this change's `outcome` signature carries the target's activity at all; epoch expansion reports `t_epoch_ms is None` rather than inferring a base when `RUN_START` is absent (INV-APV-50); malformed records are skipped and counted, never raised. `test_reader_never_imported_by_collection_path` (`gh94` 2.9) keeps the reader out of the collection path's import graph, which is what makes D-8's "read-only over the trace" structural rather than promised
- [x] 8.1a Carry `sys`/`user`/`resp`/`tool_calls` through onto the sub-event row, and expose `MOP_DATA`/`PIPELINE`/`LLM_ACK` as reader attributes beside the `RUN_START` header — **not** `RUN_END` (D5). Both were found missing on 2026-08-04 by reading the counterpart implementation against the frozen schema: `_parse_llm_call` built the typed call field-by-field without the four dump fields, and `_absorb_typed_record` skipped every run-level record but `RUN_START`. Since this reader is the sole sanctioned way to read a new trace, each omission makes its data unreachable rather than merely inconvenient — the dumps are default-on and priced by INV-SNK-13, and `wtgEdges`/`candidates` are the two quantities this stage added. **Landed as `gh94` 2.2a/2.8a**, confirmed read-only on 2026-08-04 in the counterpart source: `LlmCall` now declares `system_prompt`/`user_text`/`response`/`tool_calls` and `_parse_llm_call` populates all four from `sys`/`user`/`resp`/`tool_calls`; `TraceReader` exposes `mop_data`/`pipeline`/`llm_ack` properties beside the `RUN_START` header, each reset per pass. `RUN_END` deliberately has **no** accessor (INV-APV-53, owner decision D5) and is skipped without counting as malformed — the absence is asserted by `test_run_end_is_read_by_nothing_and_not_counted_as_malformed`, so D5's "no exit contract" is enforced by a test rather than left to discipline
- [x] 8.2 Wire `tool.py` post-run collection: gzip the raw capture to `<trace>.ndjson.gz`, non-fatal (WARNING, uncompressed trace kept); **run on the timeout path too** (before re-raising `RVToolTimeoutError`); `task.result.trace_file` is left byte-untouched; `_check_empty_trace` unchanged; zero validation/status logic (D5). **Landed in `gh94` group 3 (3.1–3.6, 6/6)**, confirmed read-only on 2026-08-04: `_gzip_trace` lives in `tools/aperv/tool.py`, appends `.ndjson.gz` rather than substituting the suffix (the counterpart's docstring gives the reason — substitution would break the `<run>.trace` ↔ archive correspondence), streams with `gzip.open` + `shutil.copyfileobj`, and is called from both the normal step-10 site and the `RVCommandTimeoutError` handler before the re-raise. The timeout call is the load-bearing one, not the belt-and-braces one: timeout is how a normal exploration run *ends*, so a gzip wired only to the success path would compress almost nothing. `gh94` 3.4 pins the trace byte-identical by hash before/after with the `.gz` decompressing to the same bytes, and monkeypatches `gzip.open` to prove the failure is status-neutral; 3.5's `test_no_collection_path_reads_run_end` keeps D5 honest on the collection side
- [x] 8.3 Migrate `aperv_tool/analysis/clock_logcat_join.py` onto the reader: replace the `[APE-STEP]` regex (`:62-63`) with reader rows, and **delete `_align_clocks()` and the whole UTC-offset reconstruction** (`:348-384` — year candidates, quarter-hour rounding, anchor choice, `alignment_residual_ms`), which the D4 heartbeat makes dead code. The join now reads step and violation from the same clock. **Blocked on 5.2a**: delete the reconstruction only after a captured run demonstrates heartbeat lines in the task's logcat file (INV-SNK-14) — deleting it first trades a working fallback for an untested mechanism. **Closed 2026-08-05. Both halves are delivered, and the note this replaces was true when written and is false now** — it said the reconstruction was "all still present and still on the live path", which was the 2026-08-04 state. What opened the gate is a measurement, not a decision: INV-SNK-14 forbade the deletion until a captured run demonstrated heartbeat lines, and `gh94` 4.0–4.2 produced that run (task `61ef88ed-2c43-4524-992b-0fd84f4ab2b4`, arm `aperv:default` on `cryptoapp.apk` at 60 s, `COMPLETED`), recorded in `openspec/changes/gh94-ndjson-trace-reader/heartbeat-evidence.md` — this change's 9.1b, where the numbers are. The reader migration is `gh94` 5.1 (`_read_steps`'s `[APE-STEP]` regex replaced by a `TraceReader`-backed `dict[int, StepRow]`) and 5.2 (`_read_heartbeats` parsing `ApeRvHb` into `(stamp, step, t_rel_ms)`), with 5.3's two fixtures. The deletion is `gh94` 5.5–5.9: `_place()` re-anchored on the heartbeat series, the reconstruction removed with `_step_timeline()` beside it, `clock_offset_ms`/`alignment_residual_ms` off `RunJoin`, and absolute time composed forward from `RUN_START.t0`. Verified in the counterpart source with `python3` rather than `grep`, because the claim is an absence: in `clock_logcat_join.py` (610 lines) `_align_clocks`, `_naive_epoch_ms`, `_QUARTER_HOUR_MS`, `clock_offset_ms`, `alignment_residual_ms` and `APE-STEP` each occur **0** times, while `ApeRvHb` occurs 7 and `_read_heartbeats` 2. The surviving mentions of a year are the opposite of a reconstruction and were read before being counted as one: `_stamp_of` builds every stamp in the placeholder year 1900 precisely because both sides of every difference come out of the same file through the same reader, so the unknown cancels instead of being inferred
- [x] 8.4 **Do not touch the frozen-corpus readers** (design D-8 carve-out): `scripts/cmpm_stratify.py`, `scripts/analyze_cmpv2_llm.py`, `experimento-cal/scripts/*`, `experimento-20260721/scripts/*`, `calibracao/*` keep parsing the legacy format, because the corpus they read is archived and will not change. Record this in the module's docs so a later P3 sweep does not read them as shims. **Landed as `gh94` 6.1/6.2**, confirmed read-only on 2026-08-04: the carve-out is written into `modules/aperv-tool/CLAUDE.md` — which scripts, why they keep the legacy parser, why that is not a P3 violation, and the operational test that separates the two populations — and `TestFrozenCorpusCarveOut` in `tests/test_aperv_tool.py` asserts both halves under INV-APV-55. The test is what makes this task's negative instruction enforceable: "do not touch these files" is not a state a later sweep can check by reading the diff, but "these paths still parse the legacy format" is
- [x] 8.5 pytest: golden NDJSON fixture → expected typed rows (field-for-field vs the design mapping table); the dump fields and the run-level attributes of 8.1a asserted present, and the absence of a `RUN_END` accessor asserted with them; `clock_logcat_join` over a new-format trace plus a heartbeat logcat reproduces the join without any offset reconstruction; gzip round-trips and its failure is non-fatal; explicitly assert no code path reads `RUN_END` for control flow. **Four of the five clauses are green; the fifth is gated — state on 2026-08-04.** Green in `gh94`: the golden fixture asserted field for field, one named test per rule (2.8); the dump fields present and absent-not-empty when the flag was off, plus `mop_data`/`pipeline`/`llm_ack` exposed (2.8a — `test_prompt_and_response_dumps_survive_the_reader`, `test_dumps_are_absent_not_empty_when_the_flag_was_off`, `test_mop_data_census_exposed`, `test_pipeline_census_says_what_was_not_assembled`, `test_llm_ack_exposed`); the `RUN_END` absence asserted alongside them (`test_run_end_is_read_by_nothing_and_not_counted_as_malformed`, `test_no_collection_path_reads_run_end`); gzip round-trip and non-fatal failure (3.4). **The fifth clause closed 2026-08-05, and it was checked for the distinction the previous session refused to blur.** *"`clock_logcat_join` over a new-format trace plus a heartbeat logcat reproduces the join **without any offset reconstruction**"* is now carried by `TestNoReconstruction` (`tests/test_clock_logcat_join.py:312`, `gh94` 5.9), and it asserts the *absence* rather than the current behaviour: `test_module_defines_no_reconstruction_machinery` is parametrized over five deleted names (`_align_clocks`, `_naive_epoch_ms`, `_read_capture_start`, `_QUARTER_HOUR_MS`, `_ANY_STAMPED_LINE`) and fails on `hasattr`, so a reintroduction fails the suite instead of lingering; `test_runjoin_carries_neither_deleted_field` reads `dataclasses.fields(RunJoin)` for `clock_offset_ms`/`alignment_residual_ms`; and `test_join_never_reads_a_non_heartbeat_line_to_anchor` states the absence behaviourally — the `ActivityManager` line that used to select the anchor is removed and every placement stays byte-identical. The "reproduces the join" half is exercised over exactly the pair the clause names: `_write_run` writes a stage-4 NDJSON trace (`RUN_START` with `t0`, `ACT`/`STATE` records defined before the steps that reference them, per INV-SNK-06) beside a logcat of matching `ApeRvHb` lines, and `TestPlacementByHeartbeat` places violations against it — including across midnight, and including the no-heartbeat run, which stays in the report as `UNALIGNED` with its denominator rather than vanishing. Measured 2026-08-05: `test_clock_logcat_join.py` + `test_trace_ndjson.py` = **70 passed**
- [x] 8.6 Run `/rv-test-run aperv-tool` (rv-android module ⇒ `rv-*` skill). This is the group's *closing* run, so it follows 8.3 and 8.5 rather than standing alone: `gh94` has already run it three times mid-group (2.11, 3.6, 5.4) and its own closing run is 5.10, still pending behind the gated deletions. **Run 2026-08-05 through the skill itself** — `/rv-test-run` is in this session's registry; the missing family is `sdd-*` (see 9.6–9.8). **304 passed, 32 skipped, 0 failures** over `modules/aperv-tool/tests/` under the CI contract (`--import-mode=importlib -o "addopts="`), at rv-android `rearch-counterparts` HEAD `7902bab4`. The 32 skips are environment-gated rather than failures: the whole migration tier asks for `APE_REPO` pointing at a checkout carrying `rearch-02-runspec`. **The warning this note used to carry is corrected, not repeated.** It said the suite "currently carries **13 failures belonging to `gh95`** (`TestVariants`/`TestArmVariants`, `KeyError: 'sata_mop_widget'`)". Measured today there are none: `-k Variant` is **9 passed**, and `sata_mop_widget` survives in exactly one file — `tests/migration/retirements.py`, as a retirement record rather than a live variant name — which is `gh95` having finished the retirement the failures were the middle of. This change's own evidence, for the comparison the old note wanted: `test_trace_ndjson.py` **34 passed** (30 on 2026-08-04), and with `test_clock_logcat_join.py` beside it **70 passed**. One caution for the next reader of this number: the module's tree moved twice while this session was in it — a 12:19 measurement gave 298/30 with another session's edits in flight, and 304/32 is the tree at HEAD

## 9. Acceptance and Verification

- [ ] 9.1 Produce a sample new-format trace (smoke run via rv-platform — never manual emulator management) covering MOP boosts, LLM calls (incl. an error and a `no_match reason=dead_pair`), a flushed final step, and `RUN_END`. **Delegated to `gh97-rearch-ab-gate` task 7.2b** (owner decision 2026-08-05: the APE-RV side executes once, in that change's smoke). The delegation carries a stated limit rather than a silent one: the smoke's three arms give MOP boosts (asserted by that change's 7.2a), an LLM call on `mop_on_llm_70`, the flushed final step and `RUN_END` — but **not** the LLM error or the `no_match reason=dead_pair`, which no arm can be made to produce inside a reduced-timeout smoke. Those two are recorded if they appear and are not required, because both are already asserted at schema level by `gh94`'s golden-fixture tests (that change's 2.8), which is where a claim about the format belongs. What is owed here is confirming that record exists, not a run
- [x] 9.1a **Throughput gate (INV-SNK-13)** — executed 2026-08-05 by the assistant via `rv-experiment`, which is the route the rv-android emulator rule prescribes rather than an exception to it (the harness owns the whole device lifecycle; no emulator was managed by hand). "Owner-executed" is how this line read while the runs were still owed; the owner authorised the assistant to drive them in session 17, swapping the jar between runs. Same APK, seed and wall-clock budget: run the **pre-change** jar twice to establish the measurement's own variation, then the **post-change** jar once. Compare steps per minute, counted **from the trace, with a counter per format** — `[APE-STEP]` lines for the pre-change jar, NDJSON `StepRecord`s for the post-change one, since either counter silently returns zero on the other format. This line said `steps` could be read from `tasks.json`; **it cannot, and the platform is right, not the task**: `TaskResult.to_dict()` (`rv-android-core/.../domain/task.py:419-442`) emits exactly the eleven tool-agnostic keys `tasks.json` carries, and a `steps` field would be APE-specific telemetry leaking into the result contract shared with Monkey, DroidBot and FastBot. Its absence is a deliberate contract, not an omission. The post-change value SHALL NOT fall below the pre-change mean by more than that variation; a larger drop blocks the stage. Record all three numbers in the verification notes whatever the outcome — this is the only measurement in the change set that observes the wall-clock cost of the new per-step write path (stdout record + logcat heartbeat + prompt-dump escaping + collection-path gzip), and the budget is wall-clock. **Stays here — it is the one obligation the `gh97` smoke cannot absorb**, because it needs three runs and two of them on the **pre-change** jar, while that smoke runs the new jar only; extending it to a paired pre/post would mean redeploying a jar mid-smoke and turning a pre-flight into a small campaign. Sequencing that this depends on and does not control: `modules/aperv-tool/src/aperv_tool/tools/aperv/ape-rv.jar` is **today the pre-change jar** — sha256 `386ce08d1846a4088755a8d755e5b70391af3b42add091d231dbcc52aed24e69`, `[APE-STEP]` present, no heartbeat, and by `docs/20260803_procedimento_worktree_rearch.md` §2 byte-identical to the jar E3 measured — the only deployed copy of it. **The first overwrite of that path is now `gh94` 4.0, not `gh97` 6.2**: the owner decision of 2026-08-05 that gave `gh94` 4.1 its own device run moved the swap earlier, so `gh94` 4.0 is what preserves the file under `backup/gh97-prechange-jar/` for exactly this run, and `gh97` 6.2a verifies the copy is there before its own rebuild. Verify it yourself before relying on it — losing it fails nothing loudly and simply makes this measurement unrunnable. **Verified 2026-08-05, by recomputation rather than by reading the digest next to the file**: `backup/gh97-prechange-jar/` holds `ape-rv.jar` (277,084 bytes), `SHA256SUMS` and `README.md`, and `sha256sum` over the file returns `386ce08d1846a4088755a8d755e5b70391af3b42add091d231dbcc52aed24e69` — matching both the stored sum and the digest this task records, so the preserved article is the E3 one. The deployed `modules/aperv-tool/.../ape-rv.jar` is now the stage-4 build (`5cebabc5…`), as expected after `gh94` 4.0. **Measured 2026-08-05 and recorded in `throughput-evidence.md` beside this file: PASS with a stated limit.** Pre-change 310 and 306 steps (mean 308, the measurement's own variation 4 steps → floor 304); post-change **335**, i.e. **+27 (+8.77 %) above the pre-change mean** — no regression to attribute, so the gate does not bind and no field is dropped. It holds on both denominators (67.0 vs 62.0/61.2 steps per minute at the nominal 5-min budget; 56.9 vs 51.1/52.3 over `execution_time_seconds`). The post count has three witnesses agreeing on 335 — parsed `StepRecord`s, `RUN_END.steps`, and 335 `ApeRvHb` logcat lines ending at t=297.7 s of the 300 s budget — while the pre-change runs have one, their jar emitting no heartbeat, which is itself confirmation the right jar ran each phase. Jars identified by recomputed sha256 before each launch, never by `build.sha`. Seed control verified as `{'seed': '42'}` on all three plus the `-s` code path (`tool.py:116`, `:1042-1044`), the plan's second witness (`:Monkey: seed=` on stdout) not being present in either format's trace. **The stated limit**: the arm was `aperv:default` on the uninstrumented APK, and a census of all 335 records found none of `dec.wtgsrc`, `dec.mopx` or `dec.comp` — the three added fields the Volume Rules caution gates on this measurement — and no LLM calls. What was priced is the base per-step path (envelope, dictionaries, omitted defaults, logcat heartbeat, collection-path gzip), which also came out ~29 % smaller per step. Pricing the three fields needs a guided arm, hence MOP data, hence the static-analysis JSON these runs skipped; it is owed to a guided-arm repeat rather than claimed here
- [x] 9.1b Confirm from the same runs that the heartbeat lines are present in the captured logcat file (INV-SNK-14), which is what unblocks task 8.3. **Delegated to `gh94-ndjson-trace-reader` tasks 4.0–4.2**, which deploy a stage-4 jar and run one short `aperv` task, grep its `task.result.logcat_file` for `ApeRvHb`, and compare the count against that run's `StepRecord` count — the same evidence `gh94` 4.1/4.2 needs, harvested once, and recorded in that change's `heartbeat-evidence.md`. This was delegated to `gh97` 7.2b until 2026-08-05, when the owner gave `gh94` its own device run rather than leave the evidence behind `gh97` 6.1 and `rearch-07`; `gh97` 7.2b survives as a per-arm confirmation across the smoke's three arms and does **not** gate that campaign, because its readers (`consolidate.py`, `verify.py`) go through `trace_ndjson` and never touch heartbeats, so an absence cannot invalidate anything they measure. **Closed 2026-08-05: the lines are there, and the numbers are recorded here rather than the delegation restated.** Run — task `61ef88ed-2c43-4524-992b-0fd84f4ab2b4`, run id `20260805T141540Z-1785987443007-abe25833`, arm `aperv:default` (`preset=aperv`, `agent=sata`) on `cryptoapp.apk`, repetition 1, 60 s timeout, `emulator-5554` headless with logcat cleared before capture, `COMPLETED` in 112 s wall clock, on the stage-4 jar sha256 `5cebabc54a5202ba216731661bd5a8d2cb291a1632d5ba6844f364af6477b657`. Counted four ways because they fail differently, and **re-counted here from the run's own artifacts rather than copied from the evidence file**: **79** raw `ApeRvHb` lines in `cryptoapp.apk__1__60__aperv.logcat` (4,528 bytes), **79** parsed by `_read_heartbeats()`, **79** `StepRecord`s read by `TraceReader` out of the 672,796-byte `.trace`, **79** distinct `s` on each side — and the two `s` sets **identical over `1..79`**, no gap, no duplicate, no heartbeat without a step and no step without a heartbeat. `RUN_START` and `RUN_END` both present; `t0=1785939340912`. Evidence and full identity: rv-android `openspec/changes/gh94-ndjson-trace-reader/heartbeat-evidence.md`; artifacts under `results/gh94_heartbeat/cryptoapp.apk/`. `gh94` 4.3's diagnoses were not triggered, and the reason is worth keeping: the tag literal agrees across both repositories — `TAG_APERV_HEARTBEAT` at `rv-android-core` `util/logging/constants.py:33`, reaching the capture allowlist at `logcat_manager.py:80`, matching `NdjsonSink.HEARTBEAT_TAG` on the jar side — which is the one thing whose failure would have been silent on both sides. INV-SNK-14's precondition is met by measurement, which is what closed 8.3. `gh97` 7.2b survives as per-arm breadth across the smoke's three arms, not as the primary record
- [ ] 9.2 Acceptance Sec. 9.11 (**gated on group 8** — the native reader is the only path to it): regenerate the 2026-07-24 calibration report tables from the sample trace via `trace_ndjson.py` and the migrated `clock_logcat_join.py` — every quantity the report consumed must be recoverable; file any gap as a schema fix, not an analysis workaround. Offline, and stays here: no device is needed, but its **input is 9.1's sample trace**, so it sequences after `gh97`'s smoke has produced one and after that change's 7.2b records where the trace was kept. The second dependency — the "migrated" `clock_logcat_join.py` this task names — **was the tighter of the two and is now satisfied** (updated 2026-08-05): `gh94` 5.5–5.9 landed once 9.1b's heartbeat evidence opened the gate, so this task is runnable in the form it was written, against the reader and the heartbeat join rather than against the reconstruction it was meant to replace. **Only the sample trace is still owed**, and it comes from 9.1 — `gh97` stood at 31/61 on 2026-08-05 with its 6.2 build and 7.2/7.2a/7.2b smoke all open, so nothing here is runnable yet. Box stays open on that one input
- [x] 9.3 Gates confirmed green, named individually so the tick says what was run: **neutrality** (Sec. 9.8) `SinkNeutralityTest` 6/6 — and it cites the strengthened form, the one that drives the sink around each `ladder()` call and asserts the load happened (records == steps, heartbeats == steps) *before* comparing sequences, not the bounded version task 6.2 started from; **round-trip and one-line** (Sec. 9.12) `JsonTest` 13/13, covering both entry points and the adversarial inputs of INV-SNK-01/02; **`RUN_END` last** (Sec. 9.7) `StatefulAgentRunEndTest` 5/5, with `StatefulAgentTearDownTest` beside it for the isolation the ordering depends on. `NdjsonSinkTest` 32/32 carries the lifecycle and volume rules under them. 52 tests across the four classes, 0 failures
- [x] 9.4 `mvn test` (full suite) and `mvn package`. **The deployment half is `gh97-rearch-ab-gate` 6.2, and it is not `mvn install`.** Two reasons, both recorded before this task: `docs/20260803_procedimento_worktree_rearch.md` §3 warns that `install` from the worktree overwrites the `ape-rv.jar` the `master` deploy shares with rv-android — the one effect that escapes the worktree — and §2 warns that a worktree build stamps `master`'s revision into `BuildInfo.GIT_SHA`. `gh97` 6.2 therefore packages with the stamp supplied on the command line and copies the jar explicitly, and its 6.2a preserves the pre-change jar 9.1a still needs. What is owed here is the green suite and a built `target/ape-rv.jar`, not a deployment. **Both halves run 2026-08-05, and the suite was measured twice on purpose.** `mvn -o test` over a pristine `git archive` export of committed HEAD `a6aa7bd7`, rather than over the shared worktree: **1131 tests, 0 failures, 19 skipped**. The worktree at the same moment gives **1123 / 0 / 19**, and the eight-test gap is not this stage's — another session has `rearch-05`'s deletion of `RunSpecCompatTest` staged (−13) alongside additions to `PresetsTest` (+3) and `RunSpecAbortTest` (+2). Both are green; the committed number is what this task owes, and the worktree number is recorded so the gap is not later read as a regression. `mvn -o package`: **BUILD SUCCESS**, producing `target/ape-rv.jar`, 294,010 bytes, sha256 `605b417441445df3a50af57ece0855c0ddc5da18c5d134547c1ef8353962f1e8`. It needs the PATH the procedure doc §3 already gives — `PATH="$ANDROID_HOME/build-tools/35.0.1:$PATH"` — because the `d8-dex` execution invokes `d8` by bare name and the SDK puts only `platform-tools`/`emulator` on the PATH; a bare `mvn -o package` fails there with `Cannot run program "d8"`, which is an environment gap and not a build defect. The jar is confirmed stage-4 by the counterpart CLAUDE.md's recipe rather than by its `build.sha` stamp (which a worktree build gets wrong — `gh94`'s evidence file records why): `strings -a classes.dex` gives `ApeRvHb`, `NdjsonSink`, `RUN_START`, `RUN_END` and `StepRecord` present, `APE-STEP` and `APE-LLM-TEL` at **0**. The previous `target/ape-rv.jar` — sha256 `5cebabc5…`, the 02:08 build whose deployed copy `gh94`'s heartbeat evidence cites — was preserved to `backup/rearch-04-group9/` before the overwrite; the deployed copy in rv-android is untouched, since only `install` copies it out of the worktree
- [x] 9.5 `CLAUDE.md` gained a `### Telemetry (the NDJSON trace)` section and `ape.telemetry` in the package map. Current-state only (P4): what a `StepRecord` is and when it closes, the discriminator rule (`{` vs `[APE] `), the out-of-step records, the defaults-omitted rule with its two exemptions, escaping by construction, and the failure latch. Three things are stated as facts about today rather than as history — telemetry is always on with **no** key selecting the sink (`ape.stepTelemetryEnabled` aborts as unknown) and the neutrality test is what makes that checkable; the heartbeat tag literal `ApeRvHb` is fixed by the counterpart's `LogcatManager.default_tags` and a mismatch is silent on both sides; and `ApeRRFormatter` survives as a reader for `ReplayAgent` only. The removed outputs are listed as *not written any more*, which a reader coming from an old run directory needs, without narrating the migration
- [x] 9.6 Run `/sdd-qa-lint-fix src/main/java`. **The skill is not in this session's registry** — the whole `sdd-*` family is absent, a standing condition of this stage rather than a failure — so the equivalent action was run directly and is named here instead of implied. There is also nothing for a lint stage to run, which 7.6 closed on and this re-verifies rather than inherits: `.sdd/sdd-config.yaml` declares `linter: none` for the Java tree, and no `checkstyle`, `spotbugs` or `pmd` configuration exists anywhere in the repository — the pom mentions none of the three. What was run in its place is a mechanical scan over the six files this stage owns (`ape/telemetry/{EventSink,NdjsonSink,NoopSink,RunCounters,StepRecord}.java` and `ape/runtime/Json.java`): unused imports **0**, tabs **0**, trailing whitespace **0**, lines past 120 columns **0**, missing final newline **0**. The one class of hit — 50 non-ASCII characters — was read before being counted as a defect, and is not one: every occurrence is an em-dash or arrow in Javadoc prose, **0** fall inside a string literal or any code, the pom sets `project.build.sourceEncoding=UTF-8`, and 78 files under `src/main/java` already carry the same convention. The learning that forbids raw special characters is about what reaches the trace, and none of these does
- [x] 9.7 Run `/sdd-verify ape`. **Not in the registry either**, so its checks were run directly — and the equivalence is stated rather than assumed, because 6.3 already recorded what that skill can and cannot do on this project: its lint and complexity stages report `skipped` for the reason 9.6 re-verified. What remains of `verify` here is the build and the suite, and both are green at HEAD `b1d4e390`: `mvn -o test` gives **1123 tests, 0 failures, 19 skipped**, and `mvn -o package` BUILD SUCCESS with the dexed jar (9.4 carries its digest and the PATH the `d8` step needs). That this differs from 9.4's 1131 is recorded there and is not a regression: `rearch-05` landed its `RunSpecCompatTest` deletion as `e1f4ca42` between the two runs, which is the move its own plan predicted
- [x] 9.8 Run `/sdd-code-reviewer`. **Not in the registry, and `/code-review` is user-triggered and billed — the assistant cannot launch it.** The review was therefore done here, and what was reviewed is named so the tick is checkable: the five files of `ape/telemetry/`, the serializer `ape/runtime/Json.java`, and the two wiring sites the invariants turn on. **No defect found**, and the four places one would most plausibly hide were each checked rather than assumed. (1) *Pooled sub-event reuse.* `llmError`/`llmBreakerOpen` set only three fields of an `LlmEvent`, so a recycled instance could have carried a previous call's `qwen`/`px`/`tok` into an error record — it cannot, because `StepRecord.nextLlmEvent()` calls `event.reset()` on every recycled instance before returning it, and `llmCount` rather than the pool's size bounds what `writeTo` iterates. (2) *Two buffers, one sink.* `recordBuf` and `sideBuf` are separate for a load-bearing reason: `outcome()` interns the target activity and state — writing `ACT`/`STATE` lines through `sideBuf` — *before* `writePending()` serializes through `recordBuf`, so a single shared buffer would corrupt the record it is mid-way through building; that same ordering is what makes INV-SNK-06 structural instead of checked. (3) *Escaping (INV-SNK-01/02).* `appendString` covers `"`, `\`, `\b\f\n\r\t`, U+2028/U+2029 and every `c < 0x20` as `\uXXXX`, with `Locale.ROOT` on the `String.format` so no Turkish-locale surprise. DEL (0x7F) and unpaired surrogates pass through unescaped; both were considered and neither can split a line, which is the property the invariant protects. (4) *Heartbeat isolation.* `writeHeartbeat` carries its own latch, distinct from the sink's, so a logcat failure costs the heartbeat and never the trace — and the early return on step re-entry means a `BadStateException` retry writes one heartbeat rather than two, which is what INV-SNK-10's "exactly one per step" requires. Confirmed rather than found: `[APE-ARCH] stages=[...]` at `DecisionPipeline.java:122` is still the single surviving string-literal emitter of the ten retired families in `src/main` — re-verified with `python3` over every `.java` under `src/main/java` with comment lines excluded, the other nine at **0** — and it stays, for the reason 7.5 gives. Teardown ordering was re-read at `StatefulAgent:1897-1905` rather than trusted from 4.2's note: `flushPendingStep` is the first `safeStep`, `runEnd` the last, every step between them inside its own

## 10. Spec Disposition Sweep (the requirements that still normatize the retired renderings)

**Why this group exists, and why it was not in the original set.** Design Context §8 performed this audit once and named four requirements outside the telemetry capabilities that normatize the retired renderings by name (`model`, `wtg-navigation`, `llm-prompt`, and `llm-routing :: Deterministic Dead-Pair Ban`). That audit was run against the main specs at `5dcf225` and it missed a second population, because the population is not static: `rearch-02` and `rearch-03` archive first, and **their** deltas restate requirements in the retired renderings too. `rearch-03`'s `llm-infrastructure` delta, for one, says in as many words that *"`LlmClient` SHALL emit the `[APE-LLM-CONFIG]` effective-manifest line exactly once at construction"* — the emitter this change's task 3.5 deleted. So the correct audit target is the **effective** spec after the two predecessors apply, not the tree as it stood when this change's design was written.

Re-run against that target on 2026-08-04 (owner-decided scope, session 6), the undispositioned set is thirteen requirements across five capabilities, every one of them false against code this change has already shipped in groups 3–5:

| Capability | Requirement | Demands | Replaced in code by |
|---|---|---|---|
| `mop-guidance` | Load Status Line and Fail-Fast; Activity-Substrate Counters on the Load Line; Handler-Join Diagnostics on the Load Line; Load memory safety; DIALOG Re-Keying; Widgetless-Substrate Classifier (INV-MOP-21/26) | `[APE-MOP-DATA]` lines | 3.4 `sink.mopData(...)` |
| `scoring-pipeline` | Scoring Pipeline Assembly from Config (INV-ARCH-04) | `[APE-ARCH] passes=[...]` | 3.4a `sink.pipeline(...)` |
| `llm-routing` | the top-level `## Invariants` block (INV-RTR-10/11/12/20); LlmRouter Lifecycle; Action Selection Pipeline | `[APE-LLM-CONFIG]`, `[APE-LLM-TEL]`, `[APE-LLM-ERROR]`, `[APE-LLM-PROMPT]`, `[APE-LLM-RESPONSE]`, `[APE-LLM-CONFIG-ACK]` | 3.3 / 3.4b / 3.5 |
| `llm-infrastructure` | LlmClient — Transport and Circuit Breaker as One Unit; SglangClient — OpenAI-Compatible HTTP Client; ScreenshotCapture — Failure-Stage Cause Seam | `[APE-LLM-CONFIG]`, `[APE-LLM-CONFIG-ACK]`, `[APE-LLM-ERROR] cause=screenshot` | 3.3 / 3.4b / 3.5 |
| `component-triggering` | INV-CT-07 | `[APE-STEP]` | 3.1 / 3.1a (`dec.comp`) |

**This is not cosmetic re-anchoring, and one entry proves it.** `mop-guidance :: Load Status Line and Fail-Fast` fixes the success line's field set as `… droppedNoId=<n> transitions=<n>` — and `transitions` is precisely the field the `event-sink` capability refuses to restore, because the three frontier passes gate on the click-only `wtgTransitions` view while that line reported `transitions.size()`, the flat list. Fourteen of the decisive campaign's 40 applications report 9–29 transitions with the whole frontier family disabled. Syncing that requirement unchanged would re-enshrine, in the main spec, the misreading `wtgEdges` exists to end.

This group gates the archive: it must complete before 9.8, since `openspec archive` is what syncs these requirements into the main specs.

- [x] 10.1 `llm-routing` delta — add an `## Invariants` section dispositioning the four top-level invariants that name retired renderings (they live outside any requirement, so they are dispositioned as `action-selection`'s `INV-SEL-*` block already is): **INV-RTR-10** retired with the manifest it describes (the `Effective LLM Config Manifest` REMOVED entry already carries the reason; the invariant needs its own disposition because it is not inside that requirement); **INV-RTR-11** re-anchored — the partition is unchanged, but "attempts that return null **without** emitting an `[APE-LLM-TEL]` line" becomes "without appending a completed-call entry to the step's `llm[]`", and the `no_match` carve-out with it; **INV-RTR-12** re-anchored onto the `LLM_ACK` record; **INV-RTR-20** re-anchored onto the surviving free-text line. Owner-decided 2026-08-04
- [x] 10.2 `llm-routing` delta — the shared-`max_tokens` clause re-anchored onto the `RUN_START` effective-plan echo; the mechanism (one read, shared by request body and echo) is unchanged, only what it is checked against moves. **The task named the wrong requirement and the correction is worth keeping**: `LlmRouter Lifecycle` no longer exists to modify — `rearch-03-decision-pipeline` REMOVES it outright when it dismantles the 996-LOC class, and moves the clause into `LLM Unit Lifecycle and Ownership`, which is what this delta actually restates. Writing the task against the main spec rather than against the effective post-predecessor spec is the same mistake that produced this whole group, reproduced inside it
- [x] 10.3 `llm-routing` delta — MODIFIED `Action Selection Pipeline`, restated over `rearch-03`'s text (which owns it after that change archives). Steps 2/4/5/6/9 and three scenarios name five retired renderings between them; each becomes its sink counterpart (`llmError`, `llmDump`, `llmAck`, `llmCall`). **No step semantics change** — this requirement is parity-gated by `rearch-03` (R8) and the `type_text`/`MODEL_LONG_CLICK` defect it deliberately preserves stays preserved, wording included
- [x] 10.4 `scoring-pipeline` delta — MODIFIED `Scoring Pipeline Assembly from Config`, restated over `rearch-03`'s seven-pass `fromParams` text: the assembly echo becomes the `PIPELINE` record, and INV-ARCH-04's "sole assembly point" clause keeps its subject while its rendering moves. `rearch-03` already anticipated this by calling `PIPELINE.candidates` a sibling of `passes`; what it did not do — could not, being the earlier change — is stop requiring the line. **Also restated here: `MopFrontierPass — Frontier Boost Toward Unvisited MOP Activities`**, which the group's opening table missed — its de-aliasing rationale rests on the `[APE-STEP]` line making the two mechanisms separable, so it names the retired rendering in the one sentence that carries its justification
- [x] 10.5 `mop-guidance` delta (**new**) — MODIFIED the load-line family onto the `MOP_DATA` record, carrying the census the `event-sink` requirement already specifies field-for-field, and **stating the `transitions` → `wtgEdges` substitution at the requirement that fixed the old field set**, not only at the new capability. Keep untouched: the fail-fast contract (`StopTestingException` when `mopDataPath` is set and load returns null, INV-MOP-22), the never-to-logcat clause (INV-MOP-21 — still true, and now for a second reason: the sink writes to `System.out`), the OOM containment (INV-MOP-26), the byte-unit comparison (INV-MOP-29), and the orphan-DIALOG count, which stays on its own `[APE-RV]` free-text line and is therefore **not** part of this sweep
- [x] 10.6 `llm-infrastructure` delta (**new**) — MODIFIED `LlmClient — Transport and Circuit Breaker as One Unit` (drop the construction-time manifest emission; the `LlmParams`-not-static-`Config` clause is untouched and is the requirement's real subject), `SglangClient — OpenAI-Compatible HTTP Client` (the server-model field is carried for the `LLM_ACK` record), and the `ScreenshotCapture — Failure-Stage Cause Seam` honesty note, whose stage+activity pair is re-anchored onto the same free-text line 10.1 gives INV-RTR-20
- [x] 10.7 `component-triggering` delta (**new**) — MODIFIED INV-CT-07, restated over `rearch-03`'s text: "exactly one `[APE-STEP]` line with `decision_source=Component`" becomes exactly one `StepRecord` with `dec.src:"Component"`, and the "no graph edge labeled by the launch" half is unchanged. The `event-sink` capability already carries the two launch scenarios (`dec.comp`), so this delta re-anchors the invariant rather than restating them
- [x] 10.8 Audit re-run to zero and `openspec validate` green. The audit is scripted rather than grepped, because the question is not "does this string appear" but "does a requirement *reachable in the synced spec* oblige it": it rebuilds the effective spec per capability by applying `rearch-02`/`-03`/`-04` deltas over the main specs in order (honouring REMOVED), then flags any requirement body carrying a `SHALL`/`MUST` sentence that names a retired rendering. Two survive and both are correct — `aperv-tool`'s reader requirement obliges the *deletion* of the `[APE-STEP]` regex, and its flow requirement asserts no legacy line is written. Narrative mentions naming a rendering as retired, and the campaign measurements quoted in the old format, are correct and stay.

**Two findings the sweep turned up beyond its brief, both fixed here.** First, this change's own `form-completion` delta restated its requirement and left a scenario reading "the `[APE-STEP]` line for that step SHALL include `form=<W_FILL>`" — a delta written against the retired rendering it exists to retire, which no per-capability review would catch because the capability *was* dispositioned. Second, `scoring-pipeline`'s `MopFrontierPass` requirement was undispositioned: it justifies the whole `mopFrontierBoost` de-aliasing by "the mechanisms are separable in the `[APE-STEP]` line", which is the sentence the field exists for. Both are now `dec.*` members, and `MopFrontierPass` gained the `dec.wtgsrc` note that completes the de-aliasing on the `wtgBoost` side.

## 11. Deletion-Side Spec Disposition (the requirements group 7 falsifies)

**Why this group exists.** Group 10 swept the requirements that still normatize the *retired renderings* — the `key=value` line families groups 3–5 replaced. It did not sweep the requirements that normatize the *deleted mechanisms* of group 7, because at the time group 7 had not been written and its targets were still live: a requirement mandating `Model.saveActionHistory` was true when the sweep ran and false the moment 7.2 landed. Running the same scripted audit against group 7's target list on 2026-08-05 — file artifacts, methods and config keys, over the effective spec after `rearch-02`/`-03`/`-04` apply — returned thirteen normative sentences, of which seven are correct (negative assertions that the artifact is *not* written, and requirements this change's own deltas already restate) and five are defects across three capabilities.

Two of the five are the exact failure class group 10 was created for, and both were invisible for the same reason: the invariant lives in the capability's top-level `## Invariants` block, **outside** any requirement, so an operation on a requirement cannot reach it. The other three are residues inside this change's own deltas.

| Capability | Item | What it demands after group 7 |
|---|---|---|
| `model` | INV-MODEL-15 (top-level invariant) | `Model.saveActionHistory` completes and produces `action-history.log` — deleted by 7.2 |
| `exploration` | INV-EXPL-29 (top-level invariant) | isolation over a step roster naming `action-history save` and the LLM summary — both retired here |
| `ui-coverage` | INV-COV-10 + `Coverage Dump Emitted First Among Teardown Writers` | the dump precedes "the action-history save", the last file writer in the chain |
| `form-completion` | this change's own delta, scenario | `stepTelemetryEnabled` is `true` — a key the same delta deletes |
| `model` | this change's own delta, Purpose + substitute (c) | `parseRect` is preserved — false since the 7.1 audit |

**`ui-coverage` is the load-bearing one, and it is a capability this change had no delta for at all.** Its requirement has now had its boundary deleted twice: `rearch-02` removed `saveGraph`, and this change removes the action-history save, after which **no teardown step writes a file**. Owner-decided 2026-08-05: re-anchor rather than dissolve — the property (the run's most fragile output is not queued behind its most expensive ones; 333 of 338 lost dumps recovered) still has content against the free-text dumps that remain, and `flushPendingStep` is explicitly outside the boundary because it is loss-bounding by the same logic that puts the dump early.

This group gates the archive for the same reason group 10 does: `openspec archive` is what syncs these requirements into the main specs.

- [x] 11.1 `model` delta — add an `## Invariants` section dissolving INV-MODEL-15 with its subject, carrying the substitutes the REMOVED requirement already records (`exploration` INV-EXPL-16/29 for teardown robustness, `event-sink` INV-SNK-03/08 for the per-step record). A per-record tolerance policy has no meaning once there is no iteration and no file
- [x] 11.2 `exploration` delta — add an `## Invariants` section re-anchoring INV-EXPL-29's step roster: the LLM summary and the action-history save leave, `flushPendingStep` and `runEnd` join, and the isolation guarantee itself is stated as untouched. Only the enumeration moves
- [x] 11.3 `ui-coverage` delta (**new capability in this change's set**) — `## Invariants` re-anchoring INV-COV-10 onto the first step that produces output (`actionCounters`), with `flushPendingStep` named as outside the boundary; MODIFIED `Coverage Dump Emitted First Among Teardown Writers`, restated by anchored extraction over the main-spec body (chain narrative, the measured-justification paragraph, the scenario title and its `THEN`, and the cut-during-serialization scenario). The 42.3 % measurement is kept as the record of why the requirement exists
- [x] 11.4 Fix the two residues inside this change's own deltas: `form-completion`'s scenario precondition naming `stepTelemetryEnabled`, and `model`'s two references to `parseRect` as preserved. Both are deltas written against a state the same change removes — the third and fourth instance of a class group 10 found twice
- [x] 11.5 Audit re-run and `openspec validate` green: **13 → 5**, and all five survivors are correct — each names a deleted mechanism precisely in order to assert it is gone (`exploration`'s two "no legacy file SHALL be written" clauses, `action-selection`'s and `scoring-pipeline`'s "the removed `ape.stepTelemetryEnabled` key aborts plan validation as unknown", and `ui-coverage`'s restated scenario naming both retired boundaries as deleted). **Two properties of the audit are what made it converge, and both are worth carrying forward.** First, it tracks *provenance*, not text: a requirement is dispositioned when this change's delta supplies its effective body or drops it, so what stays flaggable inside a restated delta is a residue — which is how the `form-completion` scenario in 11.4 was caught, on the third pass over a file two earlier passes had already corrected. Second, a delta's `## Invariants` section is kept as a note *beside* the main block rather than replacing it, because that is what archiving does with it; an earlier rebuild that replaced the block reported these invariants as clean and was wrong, which is precisely how INV-MODEL-15 and INV-COV-10 stayed invisible until now

## 12. Scenario pairing for the archive (obligation inherited 2026-08-05)

`rearch-03-decision-pipeline` archived on 2026-08-05, so the main specs this change's deltas replace
are now **its** text, not the text they were drafted against. Group 10 already anticipated the
requirement bodies (task 10.3 restated `Action Selection Pipeline` over `rearch-03`'s version); what
it did not cover is the *scenario set*, which `openspec archive` checks separately and by name.

**The three this change directly owes.** `rearch-03` added them to `action-selection :: Per-action
decision-source telemetry`, which this change also modifies, so its block must now carry them or the
archive drops them: `Stage-stamped provenance equals the StageResult label`, `Budget stage
early-return attributed`, `Launcher step attributed Component`. They are the marginal cost of that
archive, measured before and after it; nothing else in this change's set moved.

**The rest was already owed, and is larger.** A full set-diff against the current main specs puts
this change's unpaired scenarios at **51** across 11 requirements — 48 of which predate the
`rearch-03` archive entirely. The concentration is worth seeing before starting: `LLM Telemetry
Logging` (11 of 11 unpaired), `Per-action decision-source telemetry` (16 of 17), `Output Persistence
on Termination` (6 of 8), `Per-Step Decision Outcome Attribution` (4 of 6). A requirement whose
`kept` count is 0 — `LLM Telemetry Logging`, `Server Model Acknowledgement`, `StatefulAgent — LLM
Telemetry at tearDown` — is one whose scenarios were renamed wholesale, which is expected here: this
change renames the *renderings* those scenarios are named after.

**What the mechanism permits, established by `rearch-03` group 9 rather than by guesswork.** The
matcher is keyed on the scenario name inside the MODIFIED block, so a renamed scenario is
indistinguishable from a deleted one, and both apparent escapes are closed: `REMOVED` + `ADDED` of
one requirement is rejected outright, and `RENAMED` only rewrites a requirement's header line before
running the same scenario check. A renamed scenario therefore keeps the main spec's header and
carries this change's vocabulary in its body. `--no-validate` is not the answer: the guard produced
two real findings in `rearch-03`, and here it is aimed at a change whose whole subject is which
lines a run emits.

### The pairwise classification (12.2, closed 2026-08-05)

**51 unpaired → 0**, landed as five commits, one per capability. The disposition is 22 renames, 17
deliberate replacements, 12 genuine losses restated. Grouped by capability; every replacement and
every loss is named individually, because those are the ones where a reader has to be told what
happened to the claim.

| # | Capability :: Requirement | Main-spec scenario | Disposition |
|---|---|---|---|
| 1-5 | action-selection :: Per-action decision-source telemetry | `SATA-selected action attributed`, `MOP-boosted action from the EARLY_STAGE roulette attributed to MOP`, `Tie precedence MOP>MopFrontier>WTG>Menu>Form>Coverage`, `LLM early-return attributed with its channel`, `Every step is attributable when telemetry is enabled` | rename |
| 6-8 | action-selection :: Per-action decision-source telemetry | `Stage-stamped provenance equals the StageResult label`, `Budget stage early-return attributed`, `Launcher step attributed Component` | **inherited from the `rearch-03` archive — restated** (task 12.1). The provenance is still stamped by the selecting stage; only its rendering moves to `dec.src`. The launcher one gains `dec.comp`, which the retired line could not carry (written before dispatch; the record closes at step N+1) |
| 9 | action-selection :: Per-action decision-source telemetry | `MOP-frontier-driven pick attributed to MopFrontier, not WTG` | **genuine loss — restated.** The de-aliasing is asserted at pass level in `scoring-pipeline`, never at decision level after the rename |
| 10 | action-selection :: Per-action decision-source telemetry | `pick_channel discriminates short-circuit from roulette` | **genuine loss — restated.** `dec.ch` is in the requirement's prose and in no scenario |
| 11-12 | action-selection :: Per-action decision-source telemetry | `[APE-STEP] carries the MOP-screen bit`, `MOP-off arm always reports activity_has_mop consistent with MopData` | **deliberate replacement.** The bit leaves the step record entirely: it is a member of the activity's `ACT` dictionary entry, emitted once per activity and reached per step through `dec`→`act` (`event-sink :: activity_has_mop recorded once`) |
| 13-14 | action-selection :: Per-action decision-source telemetry | `click on a patch-fabricated widget is marked`, `targetless actions omit the patch bit` | **deliberate replacement** onto the `dec.patched` tri-state, asserted by `event-sink :: Tri-state patched preserved` |
| 15 | action-selection :: Per-action decision-source telemetry | `widget text with a newline stays on one line` | **deliberate replacement.** Well-formedness no longer depends on the emitter pre-flattening: the serializer escapes by construction (INV-SNK-02) |
| 16 | action-selection :: Per-action decision-source telemetry | `No [APE-STEP] lines when telemetry is disabled` | **deliberate replacement.** There is no disabled case; the scenario now asserts the abort, which is the honest content of "disabled" after this change |
| 17 | action-selection :: Per-step counterfactual attribution | `non-MOP channels carry no counterfactual fields` | rename (`fields` restored) |
| 18-19 | component-triggering :: Cadence-Based MOP Activity Launch | `invalid values clamped at load`, `launcher disabled` | rename — and the second time these two have been renamed back, `rearch-03` having done it for its own archive |
| 20 | component-triggering :: Cadence-Based MOP Activity Launch | `arm contrast is the launched set` | **genuine loss — restated**, carried forward from the main spec unmodified. It was `rearch-03`'s own rescue; dropping it here would have deleted it one archive after it was found |
| 21-22 | exploration :: Output Persistence on Termination | `abnormal termination still persists outputs`, `one failing agent-teardown step does not skip the rest` | rename |
| 23 | exploration :: Output Persistence on Termination | `saveObjModel disabled` | **genuine loss — restated.** Four retired keys abort resolution, asserted nowhere else |
| 24 | exploration :: Output Persistence on Termination | `Normal termination with defaults` | **deliberate replacement.** It asserted the coverage dump *and* `action-history.log`; the log's writer is deleted here, and the step-record stream terminated by `RUN_END` is what now records the run |
| 25 | exploration :: Output Persistence on Termination | `coverage dump precedes the first artifact write` | **deliberate replacement.** After this change no teardown step writes a file at all, so "artifact write" names nothing; the full statement is `ui-coverage`'s, against output rather than files |
| 26 | exploration :: Output Persistence on Termination | `disconnect failure does not lose the model` | **deliberate replacement.** Subject narrows for the second time (`rearch-02` took the model, this change takes the action history); the isolation it pins is untouched |
| 27 | exploration :: StatefulAgent — LLM Telemetry at tearDown | `Telemetry printed on normal termination` | **deliberate replacement.** `printSummary()` and its two lines are retired; the aggregate is a member of `RUN_END` and the ratio is derived by consumers |
| 28-33 | llm-routing :: LLM Telemetry Logging | `Matched widget logged`, `dead-pair ban visible in TEL and summary`, `Repaired tool call logged and counted`, `no_match reason separated`, `Timeout and HTTP failure discriminated`, `Circuit breaker event logged once per open episode` | rename |
| 34 | llm-routing :: LLM Telemetry Logging | `LLM call telemetry joins step and outcome on one key` | **deliberate replacement — the claim goes nowhere, deliberately.** Call, decision and outcome are members of one record, so there is no key to join on; what the join guaranteed is now structural |
| 35 | llm-routing :: LLM Telemetry Logging | `Screenshot failure emits an attributable error line` | **deliberate replacement.** The activity and capture stage stay verbatim on the surviving free-text line (INV-RTR-20), with `screenshot_failed` in `RUN_END`; it remains the one abandoned attempt with no `llm[]` sub-event |
| 36 | llm-routing :: LLM Telemetry Logging | `Stagnation mode triggered` | **deliberate replacement.** Verified rather than assumed: the `[APE-RV] LLM mode=stagnation` line it names has never existed in `src/main/java` at any commit that touched the string — `mode` has always been a *field* of the per-attempt telemetry — and the trigger condition is asserted by `Stagnation LLM Mode`, which this change does not modify |
| 37 | llm-routing :: LLM Telemetry Logging | `widget text with a newline does not break the prompt log` | **deliberate replacement**, stronger in two places: the builder still flattens (`llm-prompt`, unmodified) and the serializer escapes by construction |
| 38 | llm-routing :: LLM Telemetry Logging | `Router-side parse failure attributed without the client seam` | **genuine loss — restated.** It survived only as a parenthesis in the field table, and prose is not a gate |
| 39 | llm-routing :: Server Model Acknowledgement | `Server model acknowledged once` | rename |
| 40 | llm-routing :: Server Model Acknowledgement | `No acknowledgement without a successful response` | **genuine loss — restated** |
| 41 | llm-routing :: Action Selection Pipeline | `banned result is refused at step 10, not failed` | rename — and the step-8 placement clause this change's block had let go is restored with it |
| 42-43 | llm-routing :: Action Selection Pipeline | `Off-tree element becomes a coordinate tap`, `no_match reason is always one of three` | **genuine loss — restated.** Both are `rearch-03`'s own restatements, one archive from being dropped again |
| 44-46 | scoring-pipeline :: Per-Step Decision Outcome Attribution, Scoring Pipeline Assembly | `LLM decision attributed to a new-state discovery on a MOP screen`, `BadStateException retry emits a single outcome`, `empty pipeline under the pure arm` | rename |
| 47 | scoring-pipeline :: Per-Step Decision Outcome Attribution | `outcome on a non-MOP screen` | **deliberate replacement.** The landing-side bit is the same `ACT.mop`; the evidential link survives as `out.target`→`STATE.act`→`ACT.mop` instead of a per-step copy |
| 48 | scoring-pipeline :: Per-Step Decision Outcome Attribution | `Outcome line suppressed under pure mode` | **deliberate replacement.** No arm can suppress attribution; what protects the control arm is the neutrality gate (INV-SNK-07) — a property a test checks — rather than a switch an arm had to be trusted to set |
| 49 | scoring-pipeline :: Parity Configuration Flags | `a single gate overridden without the kill-switch` | **deliberate replacement, and the only case where the example changed rather than the claim**: it overrode the key this change deletes, so it overrides `ape.modelMenuEnabled` instead. The property — one override disturbs no other gate — is unchanged, and the substitution is stated in the scenario so it does not read as a silent edit |
| 50 | scoring-pipeline :: Parity Configuration Flags | `retired kill-switch key aborts` | **genuine loss — restated**, carried forward verbatim. It is `rearch-02`'s, and this change has nothing to say about it |
| 51 | ui-coverage :: Coverage Dump Emitted First Among Teardown Writers | `dump precedes model serialization` | rename |

**The three inherited ones cost what the handoff said they would.** Items 6-8 are the marginal cost
of the `rearch-03` archive and nothing else in this change's set moved — confirmed by the set-diff,
not assumed.

**What the sandbox archive produced** (12.4, run 2026-08-05 against a disposable copy of
`openspec/`): `+ 11, ~ 26, - 2`, fourteen capabilities updated, `event-sink` created, no abort. Eleven
restated or replaced scenarios were then confirmed present in the sandbox's synced main specs
carrying this change's bodies — the check `rearch-03` group 9 ran, for the same reason: a scenario
that pairs but syncs the wrong body is worse than one that aborts.

### The archive order, measured (2026-08-05)

`rearch-04` shares three modified requirements with `rearch-07`: `aperv-tool ::
execute_tool_specific_logic() Flow`, `component-triggering :: Cadence-Based MOP Activity Launch`,
and `mop-guidance :: MopData — Load Status Line and Fail-Fast`. Measured before/after rather than
block-against-block, which is learning 2 of the `rearch-03` write-up and matters here by 4.5×:

| Order | Cost, gross | Cost, **marginal** |
|---|---|---|
| `rearch-04` first → what `rearch-07` then owes | 9 names across the three requirements | **2** — `Gzip failure is non-fatal and write-only` and `transitions present, click edges absent`, both this change's own additions. `rearch-07`'s unpaired count goes 27 → 29 |
| `rearch-07` first → what `rearch-04` then owes | 10 names | **10.** This change's baseline is 0, so nothing absorbs them: every name is a new debt, incurred after the change has closed |

The asymmetry is the whole argument, and it is the same one `rearch-03` faced: `rearch-07` is open
(25/46) and has an unpaired set of its own to work through regardless — 27 today — so two more cost
it nothing it is not already paying. `rearch-04` going second would pay 10 into a closed change.
Two of `rearch-07`'s ten will evaporate anyway when it does its own pairing, because
`invalid values clamped at plan resolution` and `launcher absent from the plan` are renames it will
have to reverse exactly as this group just did.

`rearch-07`'s numbers are a snapshot of a change being edited in this worktree right now; the
direction of the asymmetry is stable, the second decimal is not.

- [x] 12.1 Carry `rearch-03`'s three scenarios into this change's `action-selection` block, restated
      in this change's vocabulary (they describe decision-source attribution, which the `StepRecord`
      now carries in `dec` rather than on an `[APE-STEP]` line)
- [x] 12.2 Set-diff every MODIFIED block against the current main spec and classify each unpaired
      scenario pairwise — rename / deliberate replacement / genuine loss — as `rearch-03` group 9
      did. The classification is the deliverable: a count is not one
- [x] 12.3 Restate renames under the main spec's header, replace bodies where this change
      contradicts them (stating the contradiction in the delta's prose), and restate genuine losses.
      A scenario asserting a rendering this change deletes is a **replacement**, not a loss — but say
      so in the prose, since the reader's question will be where the assertion went
- [ ] 12.4 `openspec archive rearch-04-step-ndjson-telemetry --yes` completes without aborting.
      Runnable against a sandbox copy of `openspec/` first (`cp -r openspec <scratch>/` and archive
      there) — the abort is per-capability and stops at the first failure, so the sandbox is how the
      whole list is seen at once rather than one capability per attempt. **Sandbox run green
      2026-08-05** (`+ 11, ~ 26, - 2`, fourteen capabilities, `event-sink` created; eleven restated
      bodies confirmed in the synced specs afterwards). **The real archive is deliberately not run
      here**: it is the act that ends the change, it mutates the main specs `rearch-07` is being
      written against in this same worktree, and the order measurement above — 2 against 10 — is an
      argument for going first, not an authorisation to. Owner's call

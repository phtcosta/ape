<!-- Single-module change, ~6 source files + tests. No subagent orchestration needed.
     Critical path: 1 (enum) -> 2 (LlmTapAction) -> 3 (event dispatch) -> 4 (router) -> 5 (agent wiring) -> 6 (verify).
     Groups 1-2 are foundation and must precede 3-5. Group 4 (router) is the behavioral core. -->

## 1. ActionType Enum (foundation)

- [x] 1.1 Add `MODEL_LLM_TAP` to `ape/model/ActionType.java`, positioned in ordinal order between `MODEL_MENU` and `MODEL_CLICK` (so `isModelAction()=true`, `requireTarget()=false`). Add the current-state comment explaining the ordinal placement (INV-MODEL-12, mirrors the `MODEL_MENU` slot). Do NOT change the bodies of `requireTarget()` / `isModelAction()` / `isScroll()`.
- [x] 1.2 Add unit tests (`ActionTypeTest`): `MODEL_LLM_TAP.requireTarget()==false`, `.isModelAction()==true`, `.isScroll()==false`; and that the six widget types still `requireTarget()==true` (ordinal shift regression guard).
- [x] 1.3 Run `mvn test -Dtest=ActionTypeTest` — 6/6 pass

## 2. LlmTapAction (foundation)

- [x] 2.1 Create `ape/model/LlmTapAction.java` extending `ModelAction`: constructor `LlmTapAction(State state, int pixelX, int pixelY, boolean longClick)` calling `super(state, null, ActionType.MODEL_LLM_TAP)`; fields + accessors `getPixelX()`, `getPixelY()`, `isLongClick()`. **Superseded by 7.2**: `equals`/`hashCode` are now overridden to include the payload (D3 revised after the 6.3 smoke).
- [x] 2.2 Add unit tests (`LlmTapActionTest`): constructs targetless (`getTarget()==null`, `getType()==MODEL_LLM_TAP`, `requireTarget()==false`); accessors return the coordinate/flag; getClass isolation from `MODEL_MENU`. **Superseded by 7.2**: the `twoTapsInSameStateAreEqual` case is replaced by coordinate-bearing identity.
- [x] 2.3 Run `/sdd-doc-code` — class carries full D2/D3 javadoc + self-documenting accessors; doc coverage satisfied.
- [x] 2.4 Run `mvn test -Dtest=LlmTapActionTest` — 4/4 pass

## 3. Event Dispatch

- [x] 3.1 In `MonkeySourceApe.generateEventsForActionInternal(Action)`, add `case MODEL_LLM_TAP:` — cast to `LlmTapAction`, build a zero-size `Rect(x, y, x, y)` and call `generateClickEventAt(rect, isLongClick ? LONG_CLICK_WAIT_TIME : CLICK_WAIT_TIME, ClickPoint.CENTER)`. Place the case alongside `MODEL_CLICK`/`MODEL_LONG_CLICK`. Added import `LlmTapAction`.
- [x] 3.2 Audited every `ActionType` switch in `src/main/java`. No site genuinely needs a branch: (a) `generateEventsForActionInternal` (898) — case added; (b) `validateResolvedAction` (635, `default: throw`) — reached only via `validateNewAction` over `newState.getActions()` members (callers audited: `validateAllNewActions` 1345, `validatedActionFilter` 159, `handleNullAction` 1553 — all iterate/pick from `newState`); the synthesized tap is never a member, so it never reaches this switch; (c) `StatefulAgent.onVisitStateTransition` (1307) switches over `StateTransitionVisitType`, no default — N/A; (d) `getActionBasePriority` (1399) `default: return 1` — safe; (e) recent-action prompt builder (1584) `default: "click"` — safe; (f) `ReplayAgent` (159) — a targetless model action takes the `line-156 else` branch (`getBackAction()`, same as `MODEL_MENU`), never reaching the throwing switch; taps are never serialized to replay logs anyway (out of scope); (g) `GUITreeNode` widget-actionTypes switch (152, `default: throw`) iterates a widget's own action set, which never contains the synthesized `MODEL_LLM_TAP` — unreachable, no branch needed.

## 4. Router: off-tree classification + telemetry (behavioral core)

- [x] 4.1 In `LlmRouter.mapToModelAction(...)`, at the end-of-function (after bounds-containment and Euclidean fallback both miss): if `actionType` is `"click"` or `"long_click"`, return `new LlmTapAction(state, pixelX, pixelY, "long_click".equals(actionType))`; otherwise return null. Leave the boundary-reject branch and the `back`/`type_text` handling unchanged. Added import `LlmTapAction`.
- [x] 4.2 In `LlmRouter.selectAction(...)`, classify the outcome: `match instanceof LlmTapAction` → `resultTag="llm_tap"`, increment new `llmTapCount`; widget match → `resultTag="matched"`; null → `resultTag="no_match"` with `reason=degenerate` when `parsed.getX()==0 && parsed.getY()==0`, else `reason=boundary`. Emit `reason` only on the `no_match` line (appended immediately after `result=`).
- [x] 4.3 Add `llmTapCount` field + getter; include `llm_tap=<N>` in the `[APE-RV] LLM Summary` line (`printSummary()`), and keep `matched`/`no_match`/`null`/`screenshot_failed` unchanged. Added `llmTapCount` to the `decisions` denominator: `decisions = matchedCount + llmTapCount + noMatchCount + nullCount`. Numerator stays `matchedCount`.
- [x] 4.4 Add unit tests (`LlmRouterMappingTest`): off-tree `click` → `LlmTapAction` with correct coords; off-tree `long_click` → `longClick=true`; off-tree `type_text` → null; `back` (null state → caught → null, unchanged path); boundary reject → null (no tap). Uses unresolved-node actions so both matching passes skip and the off-tree branch is reached.
- [x] 4.5 Add unit tests (`LlmRouterTelemetryTest`): summary line includes `llm_tap=<N>`; `getLlmTapCount()` defaults to 0; `llmTapCount` joins the `decisions` denominator (reflectively set matched=1/llm_tap=1 → ratio fraction `(1/2)`, not `(1/1)`). The per-decision `[APE-LLM-TEL] result=llm_tap` / `reason=degenerate|boundary` line is emitted inside device-gated `selectAction` (same exclusion as `LlmRouterTest`); validated by device smoke 6.3.
- [x] 4.6 Run `mvn test -Dtest=LlmRouterMappingTest,LlmRouterTelemetryTest` — 8/8 pass

## 5. Agent Wiring (resolve the synthesized tap — required, not a no-op)

- [x] 5.1 In each of the three `SataAgent` LLM hooks (new-state / stagnation / random), the `if (result != null)` block now delegates to a single private helper `acceptLlmResult(ModelAction)` (`SataAgent.java`, defined just above `selectNewActionNonnull`). The helper sets `DecisionSource.LLM` and, when `requiresSynthesizedResolution(result)` (i.e. `getType()==MODEL_LLM_TAP`), calls `newState.resolveAction(this, result, getThrottleForNewAction(newState, result))` before returning — the targetless branch (`State.java:393-395`) sets `resolvedGUITreeAction` exactly as for `MODEL_MENU`/`MODEL_BACK`, so `resolveNewAction()` (`StatefulAgent.java:1354-1357`) reads a non-null `GUITreeAction` (no NPE). The guard skips already-resolved matched widget actions. Throttle call is safe: the tap is built with `state==newState` (D7). Stagnation hook keeps its `graphStableCounter = 0` before the helper call. Guard extracted as the pure static `requiresSynthesizedResolution` so it is unit-testable (SataAgent is not JVM-instantiable — its selection chain touches Android).
- [x] 5.2 Added `SataAgentLlmTapTest` (3 tests) pinning the guard `SataAgent.requiresSynthesizedResolution`: `MODEL_LLM_TAP` → true (must be resolved in the hook); `MODEL_CLICK`/`MODEL_LONG_CLICK` (matched widget) → false (MUST NOT be re-resolved); `MODEL_BACK`/`MODEL_MENU` (targetless but members of `State.actions`, resolved by the normal pass) → false. The device-gated portion of the requirement — the synthesized tap acquiring a non-null `GUITreeAction` and surviving `resolveNewAction` without NPE against a live `State`/graph — is validated on the emulator by smoke 6.3, mirroring `SataAgentBackMenuCapTest`/`SataAgentDecisionSourceTest` which likewise test pure static seams only.
- [x] 5.3 Trace (source-verified, device execution deferred to 6.3): `acceptLlmResult` resolves the synthesized tap → `resolveNewAction()` (`StatefulAgent.java:1354-1357`) takes the `isModelAction()` branch, reads the now-non-null `getResolvedGUITreeAction()`, `assertNotNull` passes, returns `newAction` → `generateEventsForActionInternal` `case MODEL_LLM_TAP` (added §3.1) dispatches the raw tap → after execution `model.update(newAction, newGUITreeAction)` records a `StateTransition(source, LlmTapAction, dest)` whose `newAction.getDecisionSource()==LLM` (set in the helper). Confirmed `validateResolvedAction` (`MonkeySourceApe.java`, throwing `default`) is NOT on this path — it is reached only from `validateNewAction` over `state.getActions()` members, which the synthesized tap is not; no branch needed there.
- [x] 5.4 Run `mvn test -Dtest=SataAgentLlmTapTest` — 3/3 pass

## 6. Verification

- [x] 6.1 Run `mvn test` (full suite) — **630 tests, 0 failures, 0 errors, 19 skipped, BUILD SUCCESS**. No ordinal-shift regression (the six widget types still `requireTarget()==true`, guarded by `ActionTypeTest`). Suite has grown well past the "145" estimate in this task.
- [x] 6.2 Run `mvn package` — **BUILD SUCCESS**; `target/ape-rv.jar` (266 KB) built. `exec:d8-dex` step dexed the new `MODEL_LLM_TAP` enum value + `LlmTapAction` class with no error (the per-class `Warning in …` lines are pre-existing d8 desugaring notices present across the codebase, not failures).
- [x] 6.3 Device smoke: standalone run on a canvas app (`com.dessalines.thumbkey` or `com.gpl.rpg.AndorsTrail`) with LLM enabled; grep the `.trace` for `result=llm_tap` and confirm at least one `MODEL_LLM_TAP` edge is recorded with `DecisionSource.LLM`.
    - **Attempt 1 (2026-07-16) — FAILED, defect found; drove the D3/D8 rework in group 7.** Setup: emulator RVSec API 30, SGLang `Qwen3-VL-4B-Instruct` @ `172.18.0.2:30000`, `ape.llmPercentage=0.7`. Two facts established:
        1. *Standalone APE does not launch the app itself* — every run wedges in `waitForActivity` (4202 waits, 0 steps, 0 LLM calls). Launching the target first (`adb shell monkey -p <pkg> -c android.intent.category.LAUNCHER 1`) yields a healthy run (72 steps, 213 LLM calls). Pre-existing fork behaviour, unrelated to this change; the app must be foregrounded before the smoke.
        2. *App triage matters and is cheap.* Sampling the useful band (y∈[5%,94%]) against clickable bounds from `uiautomator dump` predicts `llm_tap` capacity: thumbkey 95.5% off-tree, saucenao 93.7%, nexttraceroute 87.4%, nerdcalci 62.7%, **AndorsTrail 0.0%** — its StartScreen is one full-screen clickable `RelativeLayout [0,0][1080,1794]`, which `mapToModelAction`'s bounds-containment always matches (105/107 calls `matched_class=RelativeLayout`). AndorsTrail can never produce an off-tree tap and is the wrong smoke target.
    - Result on thumbkey: `result=llm_tap` **emitted** (`[APE-LLM-TEL] … qwen=(500,258) pixel=(540,462) result=llm_tap matched_class=none nearest_class=Switch nearest_dist=559.0`), summary `calls=3 matched=2 llm_tap=1`. Then **crash at step 3**, `RuntimeException: sanity check failed, action should be added` (`Graph.markVisited:582` ← `StatefulAgent.updateStateInternal:736`). The tap was selected but **never dispatched** and **no edge was recorded** (`[APE] 0 MODEL_LLM_TAP` in the action histogram) — so the second PASS criterion was not met. Root cause + fix: group 7 (D8).
    - **Attempt 2 (2026-07-16, after group 7) — PASS.** thumbkey, foregrounded first, full 5 minutes, `ape.llmPercentage=0.7`. 176 steps, 122 LLM calls, **zero crashes** (`sanity check failed` / `ClassCastException` / `NullPointerException` / `Should not reach here` all 0; no "System appears to have crashed"). Summary: `calls=122 matched=91 llm_tap=29 no_match=2 null=0 breaker_trips=0`.
        - *Criterion 1* — `[APE-LLM-TEL] … qwen=(500,258) pixel=(540,462) result=llm_tap matched_class=none nearest_class=Switch nearest_dist=559.0 widgets=3 activity=com.dessalines.thumbkey.MainActivity` (29 occurrences).
        - *Criterion 2* — `[APE-STEP] step=1 … action=[-1,0][0]@MODEL_LLM_TAPg0s0[1,1][1]…@(909,1325) decision_source=LLM` (29 steps). The `[APE-STEP]` line is only the *selection* log, so the edge itself was confirmed in the persisted model graph: `sataGraph.vis.js` contains **11 `MODEL_LLM_TAP` edges** — the `StateTransition`s were recorded.
        - *D3 validated empirically* — the 11 persisted tap edges carry 11 **distinct** coordinates, including the near-miss pairs `@(71,138)`/`@(74,147)` (3–9 px apart), `@(540,1011)`/`@(540,1038)` and `@(992,1551)`/`@(995,1557)`. Under the inherited identity each pair would have collapsed into one action key and, on differing destinations, manufactured the phantom non-determinism D3 removes.
        - *D8 validated empirically* — 5 refinement attempts occurred in the run, all on **registered widget actions** (`g6a132`, `g7a143`, `g4a78`); **none** cites `MODEL_LLM_TAP`. Tap actions print an empty graph id (`[-1,0][0]`), confirming they stay out of the action inventory as designed.
        - *Note*: 916 `still waiting` lines appear mid-run (activity re-checks after the app is backgrounded/relaunched), but they did not stall the run — 176 steps completed. The launch-time wedge from attempt 1 was avoided by foregrounding the app first.
- [x] 6.4 ~~Run `/sdd-code-reviewer`~~ — **SKIPPED by explicit user instruction** (2026-07-16: "pode pular a 6.4, diga que eu falei para pular"). No code review was performed on this change; the standing evidence is the full suite (636 tests) and the 6.3 device smoke.
- [x] 6.5 Ran `/opsx:verify` + `openspec validate llm-coordinate-tap --strict` — **valid; ready for archive**. 5/5 requirements implemented, 22/24 scenarios automated (27 dedicated tests green inside the 636-test suite), D1–D8 followed, INV-MODEL-12/13/14 contiguous with the main specs' 01–11 (no collision, no clash with other open changes). Two scenarios are not JVM-testable and are covered by the 6.3 device evidence instead, as documented: *"ephemeral tap does not trigger naming refinement"* (`Model` needs `SystemClock`; predicate pinned by `ActionTypeTest`, and the run showed 5 refinements, none from a tap) and *"repeating the identical tap reuses its edge"* (equality unit-tested; edge reuse is `Graph` machinery). No CRITICAL or WARNING issues.

## 7. Ephemeral Action + Coordinate Identity (D3/D8 rework — from the 6.3 smoke crash)

<!-- Root cause: membership in State.getActions() — not ActionType — is what registers an action in the
     graph (Graph.addActions:275-284). The synthesized tap is not a member, so Graph.markVisited's
     registration assumption breaks. Audit also found the inherited identity would feed the tap's
     coordinate-driven outcomes to the CEGAR refinement as phantom non-determinism. TDD: test first. -->

- [x] 7.1 RED: added `ActionTypeTest.onlyLlmTapIsEphemeral` — `isEphemeral()` true for `MODEL_LLM_TAP`, false for `MODEL_CLICK`/`MODEL_LONG_CLICK`/`MODEL_MENU`/`MODEL_BACK` (targetless ≠ ephemeral: `MODEL_BACK`/`MODEL_MENU` **are** `State.actions` members and **are** registered). GREEN: added `Action.isEphemeral()` beside the existing `isBack()` (`Action.java:117`), with javadoc naming INV-MODEL-14, the membership rationale, and the two callers that must exempt it.
- [x] 7.2 RED: rewrote the `LlmTapActionTest` identity cases for INV-MODEL-13 — `tapsAtDifferentCoordinatesAreNotEqual`, `tapsDifferingOnlyInLongClickAreNotEqual`, `tapsWithIdenticalPayloadAreEqual` (equal `hashCode`); `tapIsNotEqualToMenuAction` (getClass isolation) kept. GREEN: overrode `equals`/`hashCode` in `LlmTapAction` extending `super` with `(pixelX, pixelY, longClick)`; class javadoc corrected (it previously asserted the opposite).
- [x] 7.3 RED: added `GraphEphemeralActionTest` (3 tests) — `markVisited(tap, ts)` does not throw and leaves both inventories empty (read reflectively); marking twice (selection + edge recording) stays clean; an unregistered **non**-ephemeral `MODEL_CLICK` still throws `action should be added`. Simpler than planned: `markVisited(ModelAction, int)` never dereferences the action's state, so a null state suffices and the `Unsafe` fixture was unnecessary. GREEN: added the `isEphemeral()` early branch to `Graph.markVisited` (`Graph.java:576`) recording `visitedAt(timestamp)` and returning, commented with INV-MODEL-14.
- [x] 7.4 Added the ephemeral guard to `Model.resolveNonDeterministicTransitions` (`Model.java:342`), beside the existing `isBack()` guard and in the same style. Single choke point — `NamingFactory.resolveNonDeterminism` is reachable only from `Model.java:347` (verified: its only caller), so no second guard. Not JVM-unit-testable (`Model` needs the Android runtime — `SystemClock`); it is a two-line early return whose predicate is pinned by 7.1.
- [x] 7.5 Run `mvn test` (full suite) — **636 tests, 0 failures, 0 errors, 19 skipped, BUILD SUCCESS** (630 → 636: +1 ephemeral predicate, +2 identity, +3 graph bookkeeping). No regression.
- [x] 7.6 Run `mvn package` — **BUILD SUCCESS**, `target/ape-rv.jar` (266577 B) rebuilt and dexed.
- [x] 7.7 Re-run the 6.3 device smoke on thumbkey (foreground the app first) — **PASS**; both criteria met, zero crashes, and D3/D8 validated on-device. Evidence recorded under 6.3, attempt 2.

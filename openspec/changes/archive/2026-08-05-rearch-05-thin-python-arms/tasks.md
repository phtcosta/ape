# Tasks — rearch-05-thin-python-arms

**Worktree** (decided 2026-08-03): all 7 stages are implemented in a git worktree on branch `rearch` (`git worktree add ../ape-rearch -b rearch`), merged into `master` only after stage 7. Setup, what the worktree inherits, and the `mvn install` caveat: `docs/20260803_procedimento_worktree_rearch.md`.

**Rewritten 2026-08-04.** The predecessor carried 52 tasks, 47 of which drove rv-android work from this repository against a stale picture of it — 27 surviving arms where `gh95-thin-python-arms` had already shipped 8, a 52-pair mapping where the module has 50, and deletion tasks for constants (`ARM_DEFINING_KEYS`, `LLM_ARM_KEYS`, `_CAL_LLM_COMMON`, `_APE_PURE_ARM_FLAGS`) that were already gone. Nineteen arms it instructed an implementer to *re-express* had been deleted. It also minted `INV-APV-40`…`44` in rv-android's invariant namespace, conflicting with `gh95`'s definitions of the same IDs. See `proposal.md` for the divergence table and `design.md` D1/D2 for why the fix is structural rather than arithmetic.

**Scope of this change, stated rather than discovered**: the rv-android side belongs to `gh95` and is done — no task below edits that repository. The jar is not modified; no `src/main` file is touched and the deployed binary is byte-identical. What lands here is a **test-tree deletion** plus two spec de-framings. Deleting a test edits the ape repo without changing the jar.

## 1. Confirm the counterpart before editing (read-only, no rv-android edits)

- [x] 1.1 Re-read the module source rather than this change's snapshot — the discipline whose absence produced the rewrite. In `rv-android/modules/aperv-tool/src/aperv_tool/tools/aperv/tool.py`, confirm: `get_variants()` returns the 8 names (`default`→`sata`, `sata_mop`, `sata_llm`, `sata_mop_llm`, `mop_on_llm_off`, `mop_off_llm_off`, `mop_on_llm_70`); `APERV_PROPERTY_MAPPING` has 50 pairs; `ARM_DEFINING_KEYS`/`LLM_ARM_KEYS`/`_CAL_LLM_COMMON`/`ape_pure`/`sata_mop_widget`/`cal_a*` are absent. If any of this has moved, correct `design.md`'s Context block **before** touching a test — the numbers there are a snapshot with a date on it, not a contract — **re-read 2026-08-05: the roster and every absence confirm exactly** (the eight names at `tool.py:326-426`, and `_APE_PURE_ARM_FLAGS` absent with the rest). One number moved and is recorded as moving rather than refreshed: the mapping is 50 pairs in the module's working tree but **51** at the committed `HEAD`, because an active `gh94` session is mid-edit in that file. No task here depends on the count, so this is a note, not a correction
- [x] 1.2 Confirm `gh95`'s state and its remaining gates: it was 37/44 on 2026-08-04 with only group 7 pending (final regeneration diff 7.1, lint, verify, review, docs-sync, owner sign-off 7.6, counterpart closure 7.7). **Task 2.x is gated on `gh95` task 7.1 having run green** — the regeneration diff is what replaces the fixtures this change deletes (design D3), so deleting them while the diff is unproven removes a net before its replacement is demonstrated, not after — **confirmed 2026-08-05: `gh95` is 56/57 and 7.1 is `[x]`, so the gate is open.** Its only remaining task is 7.6, the owner sign-off, which gates `gh95`'s archive and not this change's group 2: the net that replaces the fixtures is the diff having *run*, not the sign-off having been given
- [x] 1.3 **The dangling counterpart reference — resolved here, so nothing waits on a decision.** `gh95` refers to the ape side by task number in two places: its task 7.7 (*"Mark task 8.5a of the ape-side `rearch-05-thin-python-arms` satisfied"*) and its `proposal.md` (*"**Satisfies** task 8.5a…"*). **There is no task 8.5a in this file and there will not be one.** The obligation 8.5a described was *"open a change in rv-android carrying this stage's counterpart delta, rather than hand-editing that repo's `openspec/specs/`"* — and it is discharged **structurally**, not by a task: `gh95` is that change, it exists, and this rewrite delegates the whole matrix to it (design D1). So `gh95` 7.7 closes against this change as a whole. Record that reading here; do not create a numbered task to receive the pointer, which would re-import the coupling this rewrite removed — **and the other side reached the same reading independently**: `gh95` 7.7 is already `[x]`, its text opening *"Close the counterpart obligation … There is nothing to tick over there: its task 1.3 records that no task 8.5a exists and none will be created."* The loop is closed from both ends, by agreement rather than by a pointer
- [x] 1.3a Ask the owner to de-number `gh95`'s two references — point them at the change, not at a task ordinal, so neither side's renumbering can dangle again. **Not done by this change**: on 2026-08-04 `gh95`'s `tasks.md`, `design.md` and `specs/aperv/spec.md` all carried uncommitted owner edits, and `git add` on any of them would sweep that in-flight work into a `plan(gh95)` commit. The fix is two lines and is safe once that tree is clean; it is cosmetic in any case, because 1.3 already makes the ape side readable without it. **Status 2026-08-05: the stated blocker has lifted but the recommendation is now not to do it at all.** `openspec/changes/gh95-thin-python-arms/` is clean, so the `git add` hazard is gone — but `modules/aperv-tool/` carries uncommitted work from three live sessions (`gh94` 43/47, `gh96` 53/58, `gh97` 31/61), and the reference has stopped dangling on its own: `gh95` 7.7 is ticked and reads correctly without any renumbering. Editing another repository to fix a pointer that already resolves is the coupling this rewrite removed, performed once more. Owner's call, and the honest recommendation is to close it as unnecessary — **owner decided 2026-08-05: closed as unnecessary, no edit made.** The reasoning accepted is the one above: `gh95` 7.7 is ticked and reads correctly without any renumbering, so the reference no longer dangles, and editing another repository to repair a pointer that already resolves — while three live sessions hold uncommitted work in `modules/aperv-tool/` — would perform once more exactly the cross-repo coupling this rewrite removed. Recorded here as a decision rather than left open, so no future handoff re-raises it

## 2. Retire stage 2's transitional Python-contract scaffolding

Stage 2 pinned the jar against the *pre-change* Python output so it could deploy without touching `tool.py` (`rearch-02-runspec` group 6). `gh95` rewrote that output, so the pins now assert a contract that no longer exists — two of the five fixtures (`sata_mop_widget.properties`, `ape_pure.properties`) pin arms that have been retired outright. They are deleted here, in the stage that invalidates them, not left to rot as a frozen copy of a superseded deployment (P3). Stage 2 declared this death itself: its design says stage 5 replaces the fixtures with the real contract, and its task 6.3 says the pin holds "until stage 5".

- [x] 2.1 Delete `RunSpecCompatTest.java` (301 lines) and the five per-arm fixtures it pins — `src/test/resources/compat/{sata,sata_llm,sata_mop_llm,sata_mop_widget,ape_pure}.properties` — together with the directory's `README.md`, which documents the byte-for-byte capture and the named `tool.py` it was captured from — **done 2026-08-05, and the whole `src/test/resources/compat/` tree went with it**, negative subdirectory included, once 2.2 had restated its two fixtures inline. The file was 308 lines, not the 301 recorded here (task 4.1 already flags that drift). Deleted with `git rm` so the removal is staged rather than left as an untracked absence, and `grep -rn "CompatFixtures\|RunSpecCompatTest\|resources/compat" src/` returns nothing afterwards
- [x] 2.2 Move the retired-key coverage the fixtures also carried into `RunSpecAbortTest`, whose subject is the retired-key list itself and which was never transitional: `src/test/resources/compat/negative/{ape_pure_mode,mop_weight_activity}.properties`. Either re-home the two fixtures or restate them inline — whichever leaves `RunSpecAbortTest` self-contained. **Do this before 2.1 lands**, so the coverage is never absent from the suite even transiently — **done 2026-08-05, restated inline and landed before the deletion, in that order.** Re-homing was rejected: the class states every other plan inline through its own `entries(...)` helper, and a class that reads one resource for two cases and builds the rest by hand is the less self-contained of the two options. **What the fixtures actually added over the existing coverage is surroundings, and only that.** `everyRetiredKeyAbortsWithItsOwnReason` already iterates `KeyOwnership.retiredKeys()` — so `ape.apePureMode` and `ape.mopWeightActivity` were both already covered, each against its own reason string — and `theKillSwitchAbortsAtEitherValue` already covered `true`/`false`. Both are strictly stronger than the fixtures they would replace. What no existing case had was a retired key *hidden inside a complete, otherwise-resolvable arm*: every other abort test states the offending key alone, which leaves open the reading that classification wins only because there is nothing else to look at. So the restatement is two tests, not two file reads — `aRetiredKeyAbortsEvenInsideAnOtherwiseResolvableArm` (both keys, each inside its arm shape) and `theArmsAroundThoseRetiredKeysDoResolve`, which asserts the surroundings claim instead of describing it: drop the one retired key and the same plan resolves. Without the second, the first would pass even if the arm around the key were itself unresolvable, and would then prove nothing about classification order. `RunSpecAbortTest` 19 → 21 tests, green before 2.1 deleted anything
- [x] 2.3 Replace `PresetsTest`'s fixture-equivalence assertions (`:205-250`, every `CompatFixtures.resolve(...)` call site) with tests of the contract this stage establishes: `Presets.resolve(name)` returns the declared base vector, explicit keys override it, and the merged result passes the same validation as an explicit plan — asserted against the preset definitions, **never** against a captured copy of `tool.py`'s output. Keep every assertion in the class that does not read a fixture — **done 2026-08-05.** The five fixture-reading tests are replaced by eight contract tests, and every one of the ten assertions in the class that did not read a fixture is untouched. Four of the eight restate against `Presets.resolve(name)` what the deleted class asserted against a captured file — the resolved feature set of each preset, including the MOP family members that stay *out* (`FRONTIER`, `MOP_FRONTIER`, `ACTIVITY_TRIGGER`, `MOP_ACTIVITY_SOURCE`) and the sentinel that resolves inert on the presets without an LLM. Two are new and are the half a vector cannot show: `anExplicitKeyOverridesThePresetBaseVector` (a key stated beside the preset wins over the preset's own value — if this ever reversed, every override in the matrix would silently do nothing) and `aPresetPlusOverridesResolvesLikeTheSamePlanStatedKeyByKey`, which is the old digest equivalence with the fixture removed from it: the explicit side is now built from `Presets.resolve(MOP)` itself, so what is compared is the jar's own two statements of one plan rather than the jar against a frozen copy of another repository's output. The class javadoc went with them — it pinned an `rvsec` commit and a `tool.py` sha256, and promised that the vectors were "the only thing keeping the jar's idea of an arm and the harness's idea of an arm in agreement" until stage 5. Rewritten in current-state voice (P4), naming the roster as rv-android's. `PresetsTest` 15 → 18 tests
- [x] 2.4 Delete `CompatFixtures.java` once 2.1 and 2.3 leave it unread (it is the fixture loader, and those two are its only callers). Its constants `MOP_DATA_PATH` and `LLM_URL` are still wanted by the rewritten `PresetsTest` — re-home them rather than inlining two magic strings, and check for other readers before deleting rather than trusting this note — **done 2026-08-05, and the note checked out**: `grep -rln CompatFixtures src/` named exactly `CompatFixtures.java`, `PresetsTest.java` and `RunSpecCompatTest.java` before the deletion and nothing after it, so the two callers this task predicted were the only two. Both constants are re-homed as `private static final` fields of `PresetsTest` with their meaning kept ("path the harness pushes the artifact to; activates `MOP`" / "the SGLang endpoint a deployment supplies; activates `LLM`"), not inlined. `RunSpecAbortTest` keeps its own pre-existing `MOP_PATH` — it had one before this change and sharing one across two classes would re-create a loader dependency for one string
- [x] 2.5 **Confirm, concretely, that nothing is lost.** Before declaring 2.1–2.4 done, enumerate what `RunSpecCompatTest` asserted and show for each item either (a) `gh95`'s typed regeneration diff covers it, or (b) the rewritten `PresetsTest`/`RunSpecAbortTest` covers it. If some assertion falls in neither, **do not delete it** — record the gap and route it to `gh95` (design: Error Handling, row 3). This task is the reason the deletion is safe; do not tick it by assertion — **done 2026-08-05 by enumerating all thirteen `@Test` methods of the 308-line file. No gap; nothing was routed to `gh95`.** The audit is below, and the one thing it changed about the plan is worth stating first: the disposition splits three ways, not two. Six items are (b), three are (a), and **four have no successor at all because their subject was retired** — they assert properties of the `ape_pure` arm, which `gh95` deleted. "Covered by (a) or (b)" would have been the wrong question for those four; the honest disposition is that a test of a deleted arm is not a loss, it is a test whose subject stopped existing. Recording them as (a) would have credited `gh95`'s diff with proving something about an arm it removed.

      | # | `RunSpecCompatTest` method | Disposition |
      |---|---|---|
      | 1 | `sataResolvesAsTheBaselineArm` | **(b)** `PresetsTest.apervResolvesToTheBaselineExplorationPlan` — same feature set, same throttle, same two `assertFalse` on the absent substrates, stated against `Presets.APERV` instead of `sata.properties` |
      | 2 | `sataMopWidgetAddsTheMopSubstrate` | **(b)** `PresetsTest.mopAddsTheWeightActivatedSubstrate` — including the four MOP-family members that must stay *out*, which is the assertion most at risk of being dropped in a rewrite |
      | 3 | `sataLlmAddsTheRoutingGates` | **(b)** `PresetsTest.llmAddsTheRoutingGates` |
      | 4 | `randomLlmRoutingIsInheritedFromTheJarDefault` | **(b)** `PresetsTest.randomRoutingIsInheritedRatherThanStated`, restated one level up: the claim was "no arm dict states `ape.llmPercentage`, so both LLM arms inherit 0.02"; it is now "no *preset* states it, so both LLM presets inherit it", which is the jar-side half and the only half this repository can assert. The arm-side half is **(a)** |
      | 5 | `sataMopLlmIsTheUnionOfBothSubstrates` | **(b)** `PresetsTest.llmMopIsTheUnionOfBothSubstrates` |
      | 6 | `apePureResolvesAsAStructurallyPurePlan` | **subject retired.** `ape_pure` is one of `gh95`'s 21 retirements, and there is no `ape_pure` preset — by design, since purity is structural. The residual claim that survives it (purity is not a jar-side switch) is carried by `RunSpecAbortTest.theKillSwitchAbortsAtEitherValue`, which asserts the abort detail contains "structural" |
      | 7 | `apePureTurnsOffEveryFeatureTheBaselineArmTurnsOn` | **subject retired.** The `sata`-minus-`ape_pure` contrast has no second operand once the arm is gone |
      | 8 | `apePureAndSataStateTheSameKeysAndDifferOnlyInValues` | **subject retired**, and the surviving half is **(a)**: "both arms state the same seventeen keys" was an assertion about `_push_properties`' output shape, which is `gh95`'s regeneration diff's subject and not the jar's |
      | 9 | `noCampaignFixtureCarriesTheRetiredPurityKey` | **(a)** — an assertion about the harness's output text. `gh95` deleted the `ape_pure_mode` mapping entry outright, so no arm can emit it; the jar-side complement (a file that *does* carry it aborts) is **(b)** via items 10 and 12 |
      | 10 | `aPreEditApePureModeFileAbortsAsARetiredKey` | **(b)** `RunSpecAbortTest.everyRetiredKeyAbortsWithItsOwnReason`, which is strictly stronger — it iterates `KeyOwnership.retiredKeys()` and checks each against `KeyOwnership.retiredReason(key)`, so it cannot go stale when a key joins the list. The fixture's own contribution (the key hidden inside a full arm) is restated by 2.2's two new tests |
      | 11 | `aRetiredMopWeightAbortsRatherThanBeingIgnored` | **(b)** same iteration covers `ape.mopWeightActivity`; surroundings restated by 2.2 |
      | 12 | `apePureModeAbortsAtFalseToo` | **(b)** `RunSpecAbortTest.theKillSwitchAbortsAtEitherValue` — the same assertion, already present, already covering both values |
      | 13 | `theNoSubstrateSentinelIsInertOnTheArmsWithoutAnLlm` | **(b)** `PresetsTest.theNoSubstrateSentinelIsInertOnThePresetsWithoutAnLlm`. Worth naming separately because it is the one item where the deleted test and `gh95`'s `INV-APV-44` are looking at the same defect from two sides: the sentinel is written `-1` where the declared neutral is `-1.0`, so both a textual property comparison and a string-based inert check would be wrong. The jar-side assertion is kept |

      Two notes the enumeration produced. **First**, items 10–12 were already covered *before* this change touched anything — the retired-key coverage the fixtures "carried" was duplicated coverage, and 2.2's real content turned out to be the surroundings, not the keys (see 2.2). **Second**, the three (a) items are all assertions about the *text* `_push_properties` writes. That is the clean line this stage draws: the jar's repository keeps every claim about how a plan resolves, and none about what the harness types into a file
- [x] 2.6 Delete `rearch-02` task 6.4's verification note ("zero Python changes needed") wherever it is still cited as a live property. After this stage it is a historical fact about stage 2, not a standing guarantee — **done 2026-08-05, and the search found exactly one live citation, which group 5 was already going to rewrite.** Searched every `.md`/`.java`/`.py` in the tree with `python3` rather than `grep` (learning 7). Four kinds of hit: (i) **the one live property** — `openspec/specs/run-spec/spec.md:134`, the `THEN` of scenario `the Python edit precedes the jar`, reading *"with `_push_properties` and every other arm-dict entry untouched"*. That is the main spec, so it is a standing claim of this repository, and it is false post-`gh95`. It is replaced by task 5.2's restatement of that scenario, which keeps the half that still holds (the retired-key abort) and drops the half `gh95` falsified. **2.6 and 5.2 are therefore the same edit**, which is why nothing is deleted here separately. (ii) `docs/plans/20260802_rearchitecture_roadmap.md:287` — already written as history (*"the 'zero Python changes' premise is retired where it was false"*), a Status-log entry describing the AC1 correction; a log entry is not a live property. (iii) `docs/20260802_verificacao_p3_rearch.md` — a dated verification report, an artifact of its moment, not edited. (iv) this change's own `proposal.md`/`design.md`/`specs/run-spec/spec.md` prose, which describes the framing in order to retire it. The archived `rearch-02` `tasks.md` keeps 6.4 verbatim: an archived task list is the record of what was done, not a live guarantee, and rewriting it would destroy the provenance this stage's whole argument rests on
- [x] 2.7 Run `/sdd-test-run ape.runtime` (the whole surface of this group lives there) — **done 2026-08-05: 124 tests, 0 failures, 0 errors, 0 skipped** across the ten `ape.runtime` classes (`RunSpecAbortTest` 21, `RunSpecResolveTest` 19, `PresetsTest` 18, `FeatureDerivationTest` 14, `RunSpecEchoTest` 13, `JsonTest` 13, `RunContextTest` 9, `KeyOwnershipTotalityTest` 8, `DeviceInputChannelAbsenceTest` 7, `BuildInfoTest` 2). Run through `mvn -o test -Dtest='com.android.commands.monkey.ape.runtime.*Test'` rather than the skill, per this file's learning 11 — the `sdd-*` skills resolve paths against the primary working directory and not this worktree, and a package-scoped run is exactly what the skill would have issued

## 3. Record the correction so stages 6 and 7 do not repeat it

- [x] 3.1 Append a dated entry to the **`## Status log`** section of `docs/plans/20260802_rearchitecture_roadmap.md` (`:243`): stage 5's ape side is a test-tree deletion, the Python side was delivered by `gh95`, and the numbers that moved (27→8 arms, 2→21 retirements, 52→50 pairs). Current-state wording (P4) — a dated observation, not a narrative of the correction — **done 2026-08-05, appended at the end of the log** (entries there run chronologically ascending, so the newest goes last). It states what stage 5 *is* rather than the story of its correction: the ape side is a test-tree deletion, the Python side is `gh95`'s, the scaffolding died in the stage that invalidated it, and the suite moved 1131 → 1123. All three moved numbers are recorded, with the mapping count carried as **in motion** (50 in the working tree, 51 at committed `HEAD`, a live `gh94` session mid-edit) rather than silently refreshed to one of them — the discipline task 1.1 established
- [x] 3.1a While in that file, fix what the same drift left in it: the `## Standing constraints` entry on the `rearch-counterparts` branch (`:102`) argues its merge ordering from *"23 of 29 arms push `ape.apePureMode`"*. There are no longer 29 arms and none pushes that key — `gh95` deleted the mapping entry. **The constraint itself still holds and MUST NOT be weakened**: the hazard is a post-stage-2 jar meeting a `tool.py` that still pushes a retired key, and the deployed `tool.py` in `modules` is still the pre-`gh95` one. Correct the count, keep the ordering. **Verified 2026-08-05, and the premise holds — but say which `modules` you mean**, because `modules` is both a directory in the path and a *branch name* in `rvsec`, and the sentence is only true under the branch reading. The evidence, per branch, for `rv-android/modules/aperv-tool/src/aperv_tool/tools/aperv/tool.py`: `rearch-counterparts` 1294 lines with **no** `apePureMode`/`ape_pure` (post-`gh95`); branch `modules` 1584 lines **with** both; `origin/master` 931 lines **with** both. So the merge hazard is real and unchanged — it lives on the branches this work has not reached — while the file under `modules/aperv-tool/` on the working branch is already post-`gh95`. Write the corrected entry so a later reader cannot make the directory/branch mistake, which is easy to make and was made once while auditing this task — **done 2026-08-05, and the constraint is stronger afterwards rather than weaker.** The count is not corrected to a new figure, it is removed as a premise: the entry now argues that *one* arm pushing *one* retired key aborts that run before its first step, so the ordering holds identically at 23 arms, at one, and at any future roster. Replacing 29 with 8 would have left the same fragile shape — a rule resting on a number another repository can change — which is precisely what this stage is about. The per-branch table is reproduced in the entry itself, under an explicit **"read `modules` here as the branch, not the directory"**, with the directory reading named as the wrong conclusion it produces (that the hazard has cleared) rather than merely avoided. The constraint's discharge is stated too: it is discharged by the merge, not by the passage of time
- [x] 3.2 Record the failure mode itself, not just its instance, as a new bullet in the roadmap's **`## Standing constraints (apply to every change)`** section (`:73`) — the section that already carries the cross-repo branch rules and the invariant-dissolution rule, and the one a stage implementer reads before starting. It is a constraint, not a status entry, so it does not belong in the log. The bullet SHALL state: **a change that plans another repository's work from this one will drift, and nothing detects it** — the artifact is not next to the code, so no test, build or review in either repository fails when they diverge. The remedy is the ownership split this stage made explicit: name the counterpart change as the owner of its side and point at it, never copy its roster, its numbers or its invariant IDs. Cite this stage as the worked example (27 vs 8 arms, and `INV-APV-40`…`44` minted in both trees with conflicting content), and name **`rearch-07` against `gh96`** as the next place it can happen (2026-08-05: 38/46 against 53/58 — quote the pair as a ratio of live changes, not as frozen figures, since that is the very habit this bullet warns about) — **done 2026-08-05**, as a bullet in `## Standing constraints (apply to every change)`, immediately after the `rearch-counterparts` branch entry it is the general form of. It states the mechanism (the artifact is not next to the code, so nothing in either repository goes red when they diverge — both stay green while one describes a tree that no longer exists), the remedy (name the counterpart change as owner and point at it; never copy roster, numbers or invariant IDs, because correcting a copy only resets its clock), and this stage as the worked example with both halves — 27 vs 8 arms, and `INV-APV-40`…`44` minted in two trees with `INV-APV-42` carrying contradictory content. On `rearch-07` vs `gh96` the task's own instruction is followed literally: **no figures are quoted at all**, only the shape ("the ape side roughly two-thirds done against an rv-android side nearly closed — the same asymmetry at an earlier point") and an instruction to read both counts live. Quoting 38/46 against 53/58 in a standing constraint would have dated the bullet the same week
- [x] 3.3 Check whether `rearch-06-memory-surgical` (25/29 on 2026-08-05; its four open tasks are one JVM test and the owner-executed device memory A/B) carries any task written against the predecessor's arm picture; if so, file the correction there rather than fixing it silently here — **checked 2026-08-05: it carries none, so nothing was filed.** Swept all of `openspec/changes/rearch-06-memory-surgical/` with `python3` (learning 7) for fifteen terms that would betray the predecessor's picture — `ape_pure`, `sata_mop_widget`, `cal_a`, `27 arm`/`29 arm`, `bfs`, `APERV_PROPERTY_MAPPING`, `get_variants`, `tool.py`, `INV-APV`, `sata_mop_act_frontier`, `sata_mop_activity` and the rest. **One hit in the whole change, and it is not one**: `design.md:235`, *"no config flag, no kill-switch entry (R-rules; the `apePureMode` registry is not extended)"* — a statement that the memory fixes add no kill switch, naming the retired key as the example of what is not being created. It asserts nothing about the roster, sits in `design.md` rather than in a task, and stays true under any arm count. That `rearch-06` is clean is not luck: its subject is heap retention inside the jar, so it never had reason to describe another repository's matrix — which is itself the bullet 3.2 records, seen from the other side

## 4. Verification

- [x] 4.1 `mvn test` — full suite green with the transitional tests gone and the preset-contract tests in their place. **Measure the baseline in the same session as the deletion; do not quote one from this file.** The **1088 tests, 0 failures, 19 skipped** recorded here was true when this change was written on 2026-08-04 and is already wrong: stages 4, 6 and 7 have landed tests since (`CLAUDE.md` still says 937 — figures from three moments, none of them a contract). What this task actually asserts is a *delta*, not a total: the count falls by what `RunSpecCompatTest` contributed and holds otherwise, and only a before/after pair taken across the deletion itself can show that. **`rearch-07` has already published the expected pair and it should be checked rather than re-derived**: its group 5 measured **1131** in this tree, and its own note predicts **1118** after this change deletes `RunSpecCompatTest` — *"a session that finds 1131 after `rearch-05` lands has a merge problem rather than a lucky suite."* Treat 1118 as the prediction to confirm or falsify, and if the before-count is not 1131, find out what moved before deleting anything. (`RunSpecCompatTest.java` is 308 lines today, not the 301 task 2.1 records — the file has moved since this change was written, which is the same class of drift and the reason the count is measured rather than quoted.) **`target/` is shared with whichever sessions are live in this worktree** — check before running, or the two suites will interleave — **done 2026-08-05. Before 1131, after 1123. The prediction's mechanism is confirmed; its total is falsified, and the difference is entirely accounted for.** The baseline was measured in this session before anything was deleted and came in at exactly **1131**, matching `rearch-07`'s figure — so no merge problem, and the deletion could proceed. `RunSpecCompatTest` carried **13** `@Test` methods, so the bare-deletion arithmetic 1131 − 13 = **1118** is exactly right and `rearch-07`'s prediction holds on the part it was actually predicting. The suite reads 1123 because tasks 2.2 and 2.3 *require* replacement tests, which the prediction did not model: `RunSpecAbortTest` 19 → 21 (+2, the retired key inside a resolvable arm, and the assertion that the arm resolves without it) and `PresetsTest` 15 → 18 (−5 fixture-equivalence, +8 contract). 1131 − 13 + 5 = 1123. **The standing warning is unaffected and still worth carrying forward**: a session finding 1131 after this change lands has a merge problem, and one finding 1118 has lost the replacement tests. 0 failures, 0 errors, 19 skipped (unchanged: 13 `@Ignore` needing an Android runtime, 6 `Assume` in `SglangLiveTest`). On the shared `target/`: three `rearch-04` commits landed while this session worked, so the check was not theoretical — no suite ran concurrently, and the interleaving hazard is real
- [x] 4.2 Confirm the jar is untouched: `git diff --stat` shows no `src/main` path. A red suite in this stage cannot be a jar regression, and this task is what makes that statement checkable — **confirmed 2026-08-05, per commit rather than over a range, which turned out to matter.** This change is `e1f4ca42`, `b81713c0`, `e71984a0`, and the `rearch-04` session committed **three times in between** them (`b1d4e390`, `71410a7b`, `e8bd4013`), so a `git diff` across the range would have attributed that session's work to this one and been unreadable as evidence. Taken per commit, the three touch 13, 2 and 3 files: the `ape.runtime` test tree, `src/test/resources/compat/**`, the roadmap, and this change's own artifacts. **No `src/main` path in any of the three**, so the deployed binary is byte-identical and the statement this task exists to license — a red suite in this stage cannot be a jar regression — holds. It also confirms the handoff's concurrency rule empirically: every commit staged explicit paths and `git diff --cached --name-only` was read before each, and nothing of the other session's in-flight work was swept in
- [x] 4.3 Run `/sdd-qa-lint-fix src/test/java` — **run 2026-08-05; there is no linter for it to run, on four independent grounds**, so it fixed nothing and had nothing to fix. `checkstyle` is not on `PATH`; `pom.xml` carries no checkstyle plugin (0 references); `/google_checks.xml` does not exist; and `.sdd/sdd-config.yaml` states `linter: none`. The skill's own rule is to skip rather than install, so a skip here is the skill behaving correctly and not a gap in this change. This is the same finding `rearch-04` task 9.x recorded, reached independently — worth noting that it is a property of the project's Java tooling, not of a session. The run added one detail worth carrying and one claim worth correcting. **Detail**: even with `checkstyle` installed there is nothing to fix, because Java has no auto-fix mode in the skill's own command mapping, and the fallback ruleset would have been checkstyle's built-in `/google_checks.xml` — a convention this project never chose, so a clean run would have produced style noise against Google's rules rather than findings against the project's. **Correction**: the run reported `.sdd/sdd-config.yaml` stale in two fields, `build_system: ant` and `test_framework: none`. Only the second is stale. `build.xml` and `pom.xml` both exist in the tree, so `ant` is defensible and `rearch-04`'s session was right to say so; `test_framework: none` is wrong against a JUnit 4 suite that `mvn test` runs. **Not fixed here — it is outside this change**, and it is now recorded on both sides so the next session does not re-derive it a third time
- [x] 4.4 Run `/sdd-verify ape` — **run 2026-08-05, rooted at the worktree's absolute path per learning 11. Overall: pass.** Three stages: **tests pass** (1123 passed / 0 failed / 19 skipped); **lint skipped** on the four grounds in 4.3, which the skill's `INV-VER-03` says does not fail the run; **complexity `null`**, because the skill's language table defines no complexity tool for Java at all — distinct from a defined-but-missing one, and recorded as `null` rather than `skipped` as the skill prescribes. Summary line: `PASS: 1123 tests, lint skipped (checkstyle not installed), complexity n/a for java`. Detection resolved from `.sdd/sdd-config.yaml` (`language: java`, `mode: minimal`) at step 1, so no `sdd-detection` and no MCP call was needed
- [x] 4.5 Run `/sdd-code-reviewer` over the ape-side diff — **run 2026-08-05, scoped explicitly to this change's three commits** (`e1f4ca42`, `b81713c0`, `e71984a0`), because the concurrent `rearch-04` session's three commits are interleaved with them and a range-scoped review would have reviewed that session's work as this one's. **Verdict: comment — no blocking finding, no correctness defect, no test weakened.** Structural checks came back clean: no circular dependencies, both changed files fan-in 0, method lengths and nesting well inside thresholds. One structural gain the review credited that this change did not claim for itself: the deletion removed the package's **only** intra-directory coupling — `PresetsTest → CompatFixtures` and `RunSpecCompatTest → CompatFixtures` were the sole such edges, so the eleven remaining files now point outward at production classes and nothing sideways.

      Four warnings, all **P4 documentation drift**, and their disposition splits on the one line this change may not cross:

      - **Fixed here** — `PresetsTest`'s field comments named Python constants `gh95` deleted (`_BASELINE_ARM_FLAGS`, `_MOP_SUBSTRATE`, `_LLM_FLAGS`, `ARM_DEFINING_KEYS`). Leaving them would have been this change's own thesis violated in the file that argues it, and they contradicted the javadoc rewritten two tasks earlier. Restated to describe what each vector *is* rather than which harness constant it was translated from.
      - **Fixed here** — the re-homed `MOP_DATA_PATH` javadoc claimed to be "the path the harness pushes the compacted static-analysis JSON to", which `CLAUDE.md:189` contradicts (`/data/local/tmp/mop-artifact.json`). The value is opaque in these tests — round-tripped, never dereferenced — so the fix is to stop making the claim rather than to pick a path: which path the harness pushes to is rv-android's to state, and the divergence itself belongs to `rearch-07`, whose subject is the compact artifact.
      - **Fixed here** — `CLAUDE.md:213` still read 1131, the count this change falsified. Now 1123. A project doc stating a number this very change moved is the drift the whole stage is about, and the file was clean, so no live session's work was at risk.
      - **NOT fixed, deliberately, and routed onward** — `Presets.java:41` still advertises the fixture pin ("it resolves each preset against a fixture captured from the harness"), describing scaffolding this change deleted. **It is `src/main`.** Editing it would falsify task 4.2, which is what licenses the statement that a red suite in this stage cannot be a jar regression, and that checkable property is worth more than a corrected comment. Recorded here for whichever change next has legitimate cause to touch `src/main/java/com/android/commands/monkey/ape/runtime/Presets.java`.

      Two caveats the review raised about its own instruments, worth carrying: the **layer-violation check is vacuous rather than passing** — `.sdd/plugins/java/config.json` encodes a Spring-MVC taxonomy that classifies 1 of 24 nodes in this codebase, so "0 violations" means the check did not apply; and **import-based analysis under-reports this package**, because the tests share `com.android.commands.monkey.ape.runtime` with the code under test, so nine of ten production dependencies are same-package references with no import line. A third, unrelated to this diff but useful: `JsonTest.java:53` carries a literal NUL, so it classifies as binary and plain `grep` skips it silently — `grep -a` is needed, which is learning 7 with a specific cause
- [x] 4.6 Run `/opsx:verify rearch-05-thin-python-arms` before archiving — **run 2026-08-05: no critical issues, no warnings, ready for archive.** Completeness 21/25 at the time of the run, the four open being 4.3–4.6 themselves. Correctness 3/3 requirements dispositioned: `Explicit-Key Resolution…` is jar-side and covered by tests, while `Tool Variants` and `configure() Method` have **no ape-side implementation by design** — `grep -rn "get_variants\|_tool_config" src/` returns nothing, exactly as design D1's Mapping table predicts ("none in this repo"). Recorded as a SUGGESTION rather than a finding so a later verifier does not read that absence as a defect. Coherence: D1 followed (no arm enumerated; prohibition scoped to the requirement), **D2 followed — the change mints no `INV-APV-*`**, and the three grep hits are prose references to `gh95`'s, which is what D2 prescribes; D3 followed (net replaced before removal); D4 followed. The one substantive observation: **scenario coverage moved rather than vanished** — `current campaign arm resolves unchanged` was covered by the deleted `RunSpecCompatTest.sataMopWidgetAddsTheMopSubstrate`, and its restated body is now covered by `PresetsTest.aPresetPlusOverridesResolvesLikeTheSamePlanStatedKeyByKey`, which resolves exactly that entry map (explicit keys, no `ape.preset`) and asserts both `preset="explicit"` and the MOP feature set

## 5. Scenario pairing for the archive (found 2026-08-05, before implementation started)

**This change cannot be archived as it stands, and the number is small enough to close in one
sitting.** `openspec archive` matches scenarios **by name** inside each `MODIFIED` block against the
current main spec's requirement, and cannot tell a rename from a deletion — so any name the main
spec carries and this change's block does not aborts the sync. A set-diff of every `MODIFIED` block
against the current main specs puts this change at **6 unpaired scenarios across 3 requirements**:

| Capability :: Requirement | main | delta | kept | **unpaired** |
|---|---|---|---|---|
| aperv-tool :: Tool Variants | 2 | 4 | 0 | **2** — `Default variant resolved`, `sata_mop variant is wired (replaces Phase 4 placeholder)` |
| aperv-tool :: configure() Method | 2 | 3 | 0 | **2** — `Valid strategy configured`, `Invalid strategy raises ConfigurationError` |
| run-spec :: Explicit-Key Resolution When No Preset Is Named | 2 | 2 | 0 | **2** — `current campaign arm resolves unchanged`, `the Python edit precedes the jar` |

Every one of the three requirements has `kept = 0`: their scenarios were renamed wholesale, which is
exactly what this change does on purpose — it re-expresses arms as *preset + overrides* and de-frames
stage 2's transitional wording, so the vocabulary the old scenario names encode is the vocabulary it
retires. That makes most of these renames rather than losses, but each still needs disposition, and
two are worth checking rather than assuming: `the Python edit precedes the jar` is a **deliberate
replacement** (the ordering it asserts is the one `gh95` already performed — say where the claim
went), and `sata_mop variant is wired (replaces Phase 4 placeholder)` may be a **genuine loss**,
since the roster assertion it carries is precisely what design D1 moves out of this repository.

**The method is established by `rearch-03` group 9 and `rearch-04` group 12 — do not re-derive it.**
A renamed scenario keeps the **main spec's header** and carries this change's vocabulary in its
**body**; `REMOVED` + `ADDED` of one requirement is rejected outright and `RENAMED` only rewrites a
requirement's header before running the same scenario check, so there is no way to re-anchor a name.
`--no-validate` is not the answer: in `rearch-03` the guard produced two real findings, and in
`rearch-04` it produced four scenarios that would otherwise have been dropped one archive after
being rescued. Always dry-run in a sandbox (`cp -r openspec <scratch>/` and archive there) — the
abort is per-capability and stops at the first failure, so the sandbox is how the whole list is seen
at once.

- [x] 5.1 Set-diff every `MODIFIED` block against the current main spec and classify each of the 6
      pairwise — rename / deliberate replacement / genuine loss. The classification is the
      deliverable; a count is not one — **done 2026-08-05. The set-diff reproduced this group's
      table exactly (6 unpaired, 3 requirements, `kept = 0` on all three), and the classification
      came out four renames, two deliberate replacements, and — checking rather than assuming — no
      genuine loss.** The one this group flagged as a possible loss is not one, and the reason is
      the `rearch-03` precedent this file names: check whether an unmodified requirement already
      carries the claim.

      | Capability :: scenario | Class | Where the claim goes |
      |---|---|---|
      | `Default variant resolved` | **rename** | The claim was `get_variants()["default"]["strategy"] == "sata"` — a roster assertion, and the roster is exactly what design D1 delegates. Restated under its own header as the prohibition itself: read this requirement for the list of variants, including which is the default, and it directs you to rv-android |
      | `sata_mop variant is wired (replaces Phase 4 placeholder)` | **rename**, *not* a loss | It carried two claims. The roster half (`strategy == "sata"` for a named arm) is the delegation. The operative half — `mop_data == "static_analysis"` drives the artifact push — **is already carried by `execute_tool_specific_logic() Flow`, which this change does not modify**: step 5 of its numbered flow plus its three scenarios `sata_mop — JSON present` / `sata_mop — JSON absent` / `sata variant — no JSON push`. So nothing is lost by restating the header over the orchestration-key claim. This is the `rearch-03` pattern repeating: an apparent loss covered by a requirement the change leaves alone |
      | `Valid strategy configured` | **rename** | Same claim, new config shape: `{"strategy","throttle_ms"}` becomes `{"strategy","preset","overrides"}`. `throttle_ms` was never a real key of this contract — it is a preset value |
      | `Invalid strategy raises ConfigurationError` | **rename**, strengthened | Restated over the `bfs`/`dfs` rejection, which is the same claim made specific: the whitelist shrinks to `["sata","random"]`, so the two retired values now raise before any device interaction instead of reaching a jar that would abort on them |
      | `current campaign arm resolves unchanged` | **rename** | Its instance is unrestatable — the arm it named, `sata_mop_widget`, is one of `gh95`'s 21 retirements — so the header now carries the general rule that instance illustrated: no preset ⇒ resolve from explicit keys and jar defaults, validated identically |
      | `the Python edit precedes the jar` | **deliberate replacement**, half kept | Its `AND WHEN` half (a file carrying `ape.apePureMode` aborts with `reason=retired_key` before step 1) still holds exactly and is restated. Its first half — the four campaign arms and `ape_pure` run end-to-end *"with `_push_properties` and every other arm-dict entry untouched"* — is what `gh95` falsified by rewriting `_push_properties`, and is dropped rather than reworded. **Where the claim went**: the ordering it asserted is one `gh95` has already performed. The abort it depends on is additionally carried by the unmodified `Total Fail-Fast Validation` requirement, whose scenario `retired kill-switch key aborts with its decision` states it independently — so even the kept half is belt-and-braces, not sole custody |
- [x] 5.2 Restate renames under the main spec's header, replace bodies where this change contradicts
      them (stating the contradiction in the delta's prose), and restate genuine losses. Before
      declaring a loss, check whether an unmodified requirement already carries the claim — **done
      2026-08-05; the set-diff is at 0 and `openspec validate --strict` is clean.** Every header was
      **extracted from the main spec by a script that asserts each match is exactly one whole line**,
      never retyped, and which refuses to write if a target header is already present in the delta
      (learnings 5 and 6 — `#### Scenario: Default variant resolved` is not a prefix of anything
      here, but the guard is the point, not the luck). Four headers were re-anchored onto delta
      scenarios that already carried the right body; two had no delta counterpart and were written:
      `sata_mop variant is wired (…)` over the `mop_data` orchestration-key claim, and `the Python
      edit precedes the jar` over its surviving half. Both deltas carry a prose paragraph saying, once,
      why headers now read oddly against their bodies and that this is the archive's cost. **One thing
      the sandbox taught that is worth knowing next time: `openspec archive` prints `delta Purpose
      ignored` and does not sync a delta's `## Purpose` into the main spec** — so that prose survives
      in the archived change, and a reader of the *main* spec sees the mismatch without the
      explanation. The requirement bodies, which do sync, carry the substance
- [x] 5.3 `openspec archive rearch-05-thin-python-arms --yes` completes without aborting, verified in
      a sandbox copy first. Confirm afterwards that the restated scenarios are present in the synced
      main specs **carrying this change's bodies** — a scenario that pairs but syncs the wrong body
      is worse than one that aborts — **verified in a sandbox on 2026-08-05: the archive completes
      clean** (`aperv-tool: update`, `run-spec: update`, `Totals: + 0, ~ 3, - 0, → 0`, no abort). The
      body check was then done by reading all three synced requirements out of the sandbox's main
      specs, not by trusting the exit code, and each carries this change's text: `Tool Variants` has
      the preset+overrides definition and the roster delegation with the old five-row variant table
      and its `throttle_ms` mandate gone; `configure() Method` has the `["sata", "random"]` whitelist
      in place of `["sata", "random", "bfs", "dfs"]`; `Explicit-Key Resolution…` has the de-framed
      requirement text with stage 2's "the case for the entire current Python deployment" paragraph
      gone. **The real archive is not run here** — it is the owner's to sequence (see 5.4), and this
      task's subject is that it *would* succeed
- [x] 5.4 Before choosing when to archive, measure the order against the changes this one shares
      requirements with, as `rearch-03` and `rearch-04` both did: `rearch-07` also modifies
      `aperv-tool`. Measure **marginal** cost (unpaired before vs after the other's archive), never
      block-against-block — the two figures differed by 7× for `rearch-03` and 4.5× for `rearch-04`
      — **measured 2026-08-05 in four disposable sandboxes, in both directions, and the answer is
      that this change's archive order is free.** `rearch-04` also modifies `aperv-tool`, so it was
      measured too rather than only the one this task names.

      | Order | Marginal cost |
      |---|---|
      | `rearch-05` first → `rearch-07` | **0** (27 unpaired before, 27 after) |
      | `rearch-05` first → `rearch-04` | **0** (0 before, 0 after) |
      | `rearch-04` first → `rearch-05` | **0** (`rearch-04` archives clean; `rearch-05` still 0) |
      | `rearch-07` first → `rearch-05` | **0** |

      **Why it is free, which matters more than the zeros**: three changes modify `aperv-tool` and
      all three modify *disjoint requirements* of it. This change owns `Tool Variants` and
      `configure() Method`; `rearch-07` owns `execute_tool_specific_logic() Flow` and others. The
      pairwise interaction that cost `rearch-03` 7× and `rearch-04` 4.5× arises only when two changes
      modify the same requirement, and none of these do. So this change can be archived whenever the
      owner wants, in any order, and nothing is bought by waiting.

      **Side finding for the owner, from the same run: `rearch-07` cannot archive at all as it
      stands.** Its dry-run aborts on `aperv-tool MODIFIED failed for header "### Requirement:
      execute_tool_specific_logic() Flow" — current spec contains scenario(s) not present in the
      modified block: "sata_mop — JSON present", "sata_mop — JSON absent", "sata variant — no JSON
      push"`, and it carries **27** unpaired scenarios across four capabilities (15 of them in one
      `mop-guidance` requirement). That is `rearch-07`'s own pairing work, unaffected by this change
      and not started here. It also independently confirms the owner's 2026-08-05 decision on the
      `This specification` → `This requirement` narrowing: `execute_tool_specific_logic() Flow` is a
      requirement `rearch-07` already modifies, so had this change widened its scope to de-name that
      requirement, the two would have collided on one requirement and manufactured exactly the
      pairwise cost this measurement shows does not otherwise exist

# Tasks — activity-frontier

## 1. Config and ActionType

- [x] 1.1 Add `Config.frontierBoostWeight` (`ape.frontierBoostWeight`, default 200; 0 = off) next to `mopWeightWtg`, with a current-state comment (P4)
- [x] 1.2 Repurpose `Config.activityTriggerEnabled`: default flips to true, comment rewritten to the stagnation-launcher contract (P4 — no gh11 history; the outdated-evidence rationale lives in the proposal)
- [x] 1.3 Add `ActionType.EVENT_TRIGGER_ACTIVITY` in the `EVENT_*` group (alongside `EVENT_ACTIVATE`, **before** `MODEL_BACK`) — NOT inside the `MODEL_*` block, since `requireTarget()`/`isModelAction()` are ordinal-range checks (`ActionType.java:46-63`; range guarded by INV-EXPL-13) and a MODEL-block placement would flip both predicates `true`. Verify `requireTarget()=false`, `isModelAction()=false` with predicate unit tests

## 2. Lever A — frontier term in the WTG pass

- [x] 2.1 Add the frontier term to the WTG scoring pass in `StatefulAgent.adjustActionsByGUITree` using its **own** `MopData.getWtgTransitions(activity)` lookup with the same `widgetName`/resource-id match as the MOP boost (do NOT ride `MopScorer.scoreWtg`'s `int` return — it hides the target and fires only when MOP-reachable): on a WTG widget match, if `Graph.getActivityNode(WtgTransition.targetActivity) == null`, add `frontierBoostWeight` to the action's priority via `setPriority(getPriority() + frontierBoostWeight)` (the steering step — `wtgBoost` is telemetry-only and never enters `getPriority()`) AND record it in the action's `wtgBoost` field via read-modify-write accumulation `setWtgBoost(getWtgBoost() + frontierBoostWeight)` (so a co-occurring MOP-reach boost accumulates to combined `wtgBoost = mopWeightWtg + frontierBoostWeight`; visible in `[APE-STEP] wtg=`, exemptable by `wtgBoost > 0` downstream). This mirrors the existing WTG-MOP pass, which does both a `setPriority` increment and a `setWtgBoost` write (`StatefulAgent.java:1457-1458`)
- [x] 2.2 Emit `[APE-RV] Frontier boost: state=<a>#<key>, boosted=<n>/<total>, maxBoost=<b>` once per pass when `n > 0`
- [x] 2.3 Scoring-pass unit tests: unvisited target boosted, visited target not, weight 0 = byte-identical, stacking sum accumulated into `wtgBoost` (INV-WTG-06/-07)

## 3. Lever B — stagnation launcher

- [x] 3.1 Pure seams: `selectTriggerCandidate(activities, visitedActivities, mainActivity, rrIndex)` (exported ∧ permission==null ∧ !isMain ∧ unvisited; round-robin) and `buildDeepLinkUri(activity)` (first ACTION_VIEW filter with schemes → `scheme://host+path`; null otherwise)
- [x] 3.2 `ActivityTriggerAction extends Action` carrying className + optional deep-link URI, **plus the launch package name** captured from `MopData.getPackageName()` at construction by the launcher (so the dispatch is self-contained and `MonkeySourceApe` need not reach into `MopData`; the package still originates from `MopData.getPackageName()`, never the class name — INV-CT-04). Note: the `Action` base has **no** `DecisionSource`/`setDecisionSource` (those live only on `ModelAction`, `ModelAction.java:42-44,58,199`), so the action does NOT and cannot carry `DecisionSource.Component` itself — attribution is done in `resolveNewAction` (task 3.6). The else-branch identifies this as `Component` by the `EVENT_TRIGGER_ACTIVITY` type
- [x] 3.3 Launcher block in `SataAgent.selectNewActionNonnull` after the LLM hooks: fire only at `graphStableCounter == graphStableRestartThreshold / 2`, reset counter on success, fall through when no candidate (INV-CT-05/-06)
- [x] 3.4 Dispatch case in `MonkeySourceApe` event generation — a new `case EVENT_TRIGGER_ACTIVITY` in the real dispatch switch `generateEventsForActionInternal` (NOT the `validateResolvedAction` switch, which is `ModelAction`-only): explicit component intent `ComponentName(action.getPackageName(), action.getClassName())` (the package the `ActivityTriggerAction` captured from `MopData.getPackageName()`, never from the class name — main-spec INV-CT-04), or `ACTION_VIEW` + `Uri.parse(deepLink)` **constrained to the app via `intent.setPackage(action.getPackageName())`** when a deep-link is present, via `AndroidDevice.startActivity` (which adds `FLAG_ACTIVITY_NEW_TASK`); WARNING on failure, run continues
- [x] 3.5 Remove activities from the probabilistic pool unconditionally — P3. Delete: (a) the `activityTriggerEnabled` branch in `buildTriggerTuples` (`StatefulAgent.java:1038-1040`, `if (Config.activityTriggerEnabled) { candidates.addAll(data.getActivities()); }`); (b) the now-dead activity skip at `:1043` (`if ("activity".equals(c.componentType) && !c.exported) continue;`) — vacuous once no activity can enter `candidates`; (c) the now-dead dispatch arm at `:1140-1142` (`else if (c instanceof ComponentInfo.ActivityInfo) return AndroidDevice.startActivity(intent);`) — the tuple path can no longer carry an `ActivityInfo`; the stagnation launcher's own dispatch (task 3.4) is the sole `startActivity` path. Rewrite the INV-MOP-15 javadoc (`:1022-1030`) to stop naming activities — the tuple builder handles receivers/services (and providers via the separate provider path) only (P4)
- [x] 3.6 **Teach `resolveNewAction`'s non-model else-branch (`StatefulAgent.java:1308-1315`) to read the decision source from the action instead of hardcoding `ModelAction.DecisionSource.SATA`** — attribute `EVENT_TRIGGER_ACTIVITY` as `Component` (special-case the type, or read a source `ActivityTriggerAction` exposes). This is REQUIRED for INV-CT-07: without it every launch's `[APE-STEP]` line emits `SATA`, and `decision_source=Component` (a currently-dead enum value) is never produced. Add a decision-source test asserting the launcher path emits exactly one `[APE-STEP]` with `decision_source=Component` (INV-CT-07 / INV-SEL-04)

## 4. Unit tests (JVM)

- [x] 4.1 Candidate matrix: exported/permission/main/visited combinations, round-robin wrap, fire-time (not build-time) visited check (INV-CT-06)
- [x] 4.2 Deep-link URI building: scheme-only, scheme+host, scheme+host+path, VIEW-less filters → null, empty schemes → null (INV-CT-07 dispatch precondition)
- [x] 4.3 Gate predicate: fires only at the equality point; disabled flag → never; no candidate → no counter reset (INV-CT-05/-08)
- [x] 4.4 Fix `StatefulAgentTriggerTest` for unconditional activity-exclusion (`buildTriggerTuples` no longer has the `activityTriggerEnabled` branch): (a) **test 19.6 `testActivityTriggerDisabledExcludesActivitiesFromTupleList` (`:110-133`)** — its second half sets `activityTriggerEnabled=true` and asserts `assertTrue("activity present when enabled", hasActivity)`, which becomes a HARD FAILURE once the branch is gone; delete that enabled→present half (keep the disabled→absent assertion, now unconditional), or delete the test; (b) **test 19.2 `testTriggerSkipsNonExportedActivities` (`:54-66`)** — stays green but becomes VACUOUS (activities are never in the pool regardless of `exported`); either update it to assert activities are always excluded, or annotate it as intentionally-vacuous/remove it
- [ ] 4.5 Update `INV-EXPL-05`'s non-model enumeration in the **main** `exploration` spec to include `EVENT_TRIGGER_ACTIVITY` (it lives under a requirement this delta does not otherwise MODIFY, so it is flagged here and applied at sync/archive time — see the exploration delta note); keep it consistent with the modified `ActionType Classification` list
- [x] 4.6 Run the new/updated test classes
- [ ] 4.7 At sync/archive time, rewrite the **main** `component-triggering` spec so post-archive main does not self-contradict this delta ("Activities excluded" vs the stagnation launcher). Two edits, mirroring task 4.5's pattern (both live under requirements/prose this delta does not restate as MODIFIED, so they are flagged here and applied at sync/archive time): (a) **INV-CT-03** (`spec.md:38`, currently "Only BroadcastReceivers and Services SHALL be triggered. Activities and ContentProviders are excluded.") — reword to preserve the original intent for the probabilistic/tuple path (only BroadcastReceivers and Services SHALL enter the `buildTriggerTuples` pool; ContentProviders keep their separate provider path; activities SHALL NOT enter the probabilistic pool under any config) while carving out the stagnation launcher as the sole activity path (`EVENT_TRIGGER_ACTIVITY`, `decision_source=Component`, per this delta's Stagnation-Triggered Activity Launch requirement); (b) the **purpose clause** (`spec.md:8`, "Activities are excluded from triggering because … triggering them via `startActivity()` disrupts the SATA exploration flow") — reword so it states activities are excluded from the *probabilistic* pool but are handled by the stagnation launcher (model-visible, stagnation-gated, manifest-filtered)

## 5. Verification

- [x] 5.1 Full suite: `mvn test` (0 failures/errors)
- [x] 5.2 `openspec validate activity-frontier --strict`
- [ ] 5.3 Device smoke (rebuilt jar): deep app from the depth cohort (e.g. `dev.ukanth.ufirewall_20260301.apk`, 4/32 activities in cmpft2) — `Frontier boost` lines present; on stagnation, one `[APE-STEP] decision_source=Component` + `Triggering activity` dispatch line; cov_act rises vs the cmpft2 trace of the same APK; no crash storm; a simple 1-activity app emits neither. Also assert the fair-test config obligation: because both levers are inert without `mopDataPath` and default-on, any non-MOP baseline arm MUST run with `ape.frontierBoostWeight=0` AND `ape.activityTriggerEnabled=false` — the smoke SHALL include a baseline-arm config check (grep the baseline arm's experiment config for both settings) so the confound cannot slip in silently
